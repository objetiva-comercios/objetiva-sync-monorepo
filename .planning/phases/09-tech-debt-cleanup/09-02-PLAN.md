---
phase: 09-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - (deletions only - no source code files modified)
autonomous: true

must_haves:
  truths:
    - "No .mjs test scripts exist in monorepo root, gateway root, or sync module root"
    - "No .backup or .bak files exist anywhere in the repository (excluding node_modules)"
    - "No debug output .txt files or isolated progress-tracking .md files exist in module roots"
    - "Legitimate files are preserved: .planning/ directory, README.md, package.json, source code"
  artifacts:
    - path: "objetiva-sync-monorepo/ (root)"
      provides: "Clean monorepo root without test scripts or log files"
    - path: "objetiva-sync-gateway/ (root)"
      provides: "Clean gateway root without temp scripts, output logs, or isolated .md tracking files"
    - path: "objetiva-sync/ (root)"
      provides: "Clean sync module root without test scripts, log files, or debug artifacts"
  key_links:
    - from: "cleanup verification"
      to: "git status"
      via: "All deleted files show as deleted in git diff"
      pattern: "deleted:"
---

<objective>
Remove all temporary scripts, backup files, output logs, and development garbage from both modules and the monorepo root.

Purpose: A clean repository is prerequisite for deployment (Phase 11). Development artifacts like .mjs test scripts, .backup files, debug output .txt files, and isolated progress-tracking .md files must be removed before the release candidate. Git history preserves all previous versions.

Output: Clean repository with only production source code, configuration, planning docs, and legitimate documentation.
</objective>

<execution_context>
@C:\Users\Administrador\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Administrador\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tech-debt-cleanup/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove temporary test scripts and debug artifacts from all module roots</name>
  <files>(deletions only)</files>
  <action>
    Delete the following files. Use `rm` for each file explicitly (NOT wildcard patterns) to prevent accidental deletion of production files.

    **Monorepo root (C:/Users/sistemas/.proyectos/objetiva-sync-monorepo/):**

    .mjs test scripts (12 files):
    - fix-jwt-expiration.mjs
    - fix-sync-dashboard.mjs
    - test-cancel-complete.mjs
    - test-cancel-immediately.mjs
    - test-cancel-status.mjs
    - test-cancel-with-auth.mjs
    - test-fetch-endurance.mjs
    - test-fetch-endurance2.mjs
    - test-fetch-endurance3.mjs
    - test-gateway-connection.mjs
    - update-ingestion.mjs
    - update-routes.mjs

    Debug/log files (3 files):
    - gateway-sync-log.txt
    - sync-service-log.txt
    - NUL (empty Windows artifact)

    State temp files (2 files):
    - .planning/STATE.md.bak
    - .planning/STATE.md.temp

    Misc planning artifacts (3 files):
    - .planning/v1-INTEGRATION-CHECK.md
    - .planning/v1-INTEGRATION-SUMMARY.txt
    - .planning/v1-MILESTONE-AUDIT.md

    Other (1 file):
    - .ultimo-estado.txt

    **Gateway root (objetiva-sync-gateway/):**

    .mjs test scripts (4 files):
    - check-schema.mjs
    - test-parser.mjs
    - test-schema-endpoint.mjs
    - verify-data.mjs

    Shell test scripts (2 files):
    - test-schema-endpoint.sh
    - kill-port.sh

    JavaScript fix scripts (1 file):
    - fix-pago-minimal.js

    Shell deployment/migration scripts (3 files):
    - deploy.sh
    - migrate-to-snake-case.sh
    - setup.sh

    Debug output files (4 files):
    - dry-run-output.txt
    - entity-output.txt
    - regenerate-output.txt
    - regenerate-output-fixed.txt

    Help/notes files (1 file):
    - AYUDA.txt

    Isolated progress-tracking .md files to DELETE (5 files):
    - CAMBIOS-SCHEMA.md
    - CHANGELOG_2024-12-27.md
    - GUIA-REGENERACION-SCHEMAS.md
    - PROGRESO.md
    - RETOMAR_TRABAJO.md

    .md files to KEEP (leave untouched):
    - README.md (project documentation)
    - DEPLOYMENT.md (deployment guide -- needed for Phase 11)
    - SETUP.md (setup instructions -- useful reference)

    **Sync module root (objetiva-sync/):**

    .mjs test/fix scripts (10 files):
    - add-hide-sync-progress.mjs
    - debug-cancel-flow.mjs
    - fix-cancel-button.mjs
    - fix-cancel-simple.mjs
    - fix-cancel-sync-properly.mjs
    - fix-complete-sync-race-condition.mjs
    - fix-update-progress-ignore-canceled.mjs
    - reset-password.mjs
    - test-cancel-e2e.mjs
    - test-cancel-flow.mjs

    Shell scripts (1 file):
    - kill-port.sh

    Debug/log files (2 files):
    - cookies.txt
    - server-log.txt

    Windows artifacts (1 file):
    - NUL

    After deleting all files, verify each deletion by running `ls` on the specific file path to confirm it no longer exists.
  </action>
  <verify>
    Run: `ls *.mjs 2>/dev/null` in monorepo root -- should produce no output
    Run: `ls objetiva-sync-gateway/*.mjs 2>/dev/null` -- should produce no output
    Run: `ls objetiva-sync/*.mjs 2>/dev/null` -- should produce no output
    Run: `ls *.txt 2>/dev/null` in monorepo root -- should produce no output (except possibly package-lock.json-related)
    Run: `find . -name "*.mjs" -not -path "*/node_modules/*" | wc -l` -- should be 0
  </verify>
  <done>
    All temporary .mjs scripts, debug output .txt files, shell scripts, and isolated progress-tracking .md files are deleted from monorepo root, gateway root, and sync module root. Legitimate files (README.md, DEPLOYMENT.md, SETUP.md, source code) are preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove all backup files across the repository</name>
  <files>(deletions only)</files>
  <action>
    Delete ALL .backup and .bak files found in the repository. These are development snapshots that are no longer needed -- git history provides version recovery.

    **Files to delete (11 total):**

    Planning backups (1 file):
    - .planning/STATE.md.bak (already listed in Task 1, skip if already deleted)

    Sync module backup files (6 files):
    - objetiva-sync/src/dashboard/routes/api/queries.ts.backup
    - objetiva-sync/src/dashboard/routes/api/sync.ts.bak
    - objetiva-sync/src/dashboard/views/logs/index.ejs.backup
    - objetiva-sync/src/store/repositories/sync-logs-repo.ts.backup
    - objetiva-sync/src/sync/batch-processor.ts.backup
    - objetiva-sync/tests/helpers/test-db.ts.bak
    - objetiva-sync/tests/integration/articulos.integration.test.ts.bak

    Gateway backup files (3 files):
    - objetiva-sync-gateway/.env.backup
    - objetiva-sync-gateway/package.json.backup
    - objetiva-sync-gateway/prisma/schema.prisma.backup

    NOTE on schema.prisma.backup: Plan 09-01 uses this file as reference for restoring Prisma models. If 09-01 runs in parallel, this file should only be deleted AFTER 09-01 reads it. Since plans are in Wave 1 (parallel), check if schema.prisma.backup still exists before deleting. If 09-01 has not yet completed its restore, leave the file and note it. In practice, since the Prisma schema content is well-documented in the research notes, this dependency is soft -- proceed with deletion.

    Also delete any leftover `.residual` test directories:
    - objetiva-sync/src/.residual-md-tests(borrar)/  (entire directory)

    After deleting all files, verify cleanup:
    ```bash
    find . \( -name "*.backup" -o -name "*.bak" \) -not -path "*/node_modules/*" | wc -l
    ```
    Must output 0.
  </action>
  <verify>
    Run: `find . \( -name "*.backup" -o -name "*.bak" \) -not -path "*/node_modules/*"` -- should produce no output
    Run: `ls objetiva-sync/src/.residual-md-tests* 2>/dev/null` -- should produce no output
    Run: `find . -name "*.mjs" -not -path "*/node_modules/*" | wc -l` -- should be 0 (Task 1 verification repeated)
  </verify>
  <done>
    Zero .backup and .bak files remain in the repository (excluding node_modules). The .residual-md-tests directory is deleted. Combined with Task 1, the repository contains only production source code, configuration, planning docs, and legitimate documentation.
  </done>
</task>

</tasks>

<verification>
1. `find . -maxdepth 1 -name "*.mjs" | wc -l` -- outputs 0 (monorepo root clean)
2. `find objetiva-sync-gateway -maxdepth 1 -name "*.mjs" | wc -l` -- outputs 0 (gateway root clean)
3. `find objetiva-sync -maxdepth 1 -name "*.mjs" | wc -l` -- outputs 0 (sync root clean)
4. `find . \( -name "*.backup" -o -name "*.bak" \) -not -path "*/node_modules/*" | wc -l` -- outputs 0
5. `find . -maxdepth 1 -name "*.txt" -not -name "package-lock*" | wc -l` -- outputs 0 (root clean of log files)
6. `ls objetiva-sync-gateway/PROGRESO.md 2>/dev/null` -- no output (isolated .md removed)
7. `ls objetiva-sync-gateway/README.md` -- exists (legitimate docs preserved)
8. `ls objetiva-sync-gateway/DEPLOYMENT.md` -- exists (legitimate docs preserved)
9. `ls .planning/ROADMAP.md` -- exists (planning docs preserved)
</verification>

<success_criteria>
- Zero .mjs files in monorepo root, gateway root, or sync module root
- Zero .backup or .bak files anywhere in repository (excluding node_modules)
- Zero debug .txt files in monorepo root
- Zero isolated progress-tracking .md files in gateway root (PROGRESO.md, RETOMAR_TRABAJO.md, etc.)
- README.md, DEPLOYMENT.md, SETUP.md preserved in gateway root
- .planning/ directory fully preserved
- All production source code, configs, and package.json files untouched
</success_criteria>

<output>
After completion, create `.planning/phases/09-tech-debt-cleanup/09-02-SUMMARY.md`
</output>
