---
phase: 25-script-adaptation-207-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - objetiva-sync/src/api-client/articulos-client.ts
  - objetiva-sync/src/api-client/comprobantes-cabecera-client.ts
  - objetiva-sync/src/api-client/comprobantes-detalle-client.ts
  - objetiva-sync/src/api-client/comprobantes-pagos-client.ts
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "When a sync batch receives HTTP 207 with errors.length === 0, the client returns success: true"
    - "When a sync batch receives HTTP 207 with errors.length > 0, the client returns success: false"
    - "When 207 has 0 errors, logger.info is called with message containing 'Batch exitoso, sin errores'"
    - "When 207 has actual errors, logger.warn is called with message containing 'Batch con exito parcial (207 Multi-Status)'"
    - "All 4 entity clients have the same fix applied"
  artifacts:
    - path: "objetiva-sync/src/api-client/articulos-client.ts"
      provides: "Fixed 207 handling for articulos"
      contains: "success: !hasErrors"
    - path: "objetiva-sync/src/api-client/comprobantes-cabecera-client.ts"
      provides: "Fixed 207 handling for comprobantes cabecera"
      contains: "success: !hasErrors"
    - path: "objetiva-sync/src/api-client/comprobantes-detalle-client.ts"
      provides: "Fixed 207 handling for comprobantes detalle"
      contains: "success: !hasErrors"
    - path: "objetiva-sync/src/api-client/comprobantes-pagos-client.ts"
      provides: "Fixed 207 handling for comprobantes pagos"
      contains: "success: !hasErrors"
  key_links:
    - from: "articulos-client.ts"
      to: "logger"
      via: "conditional log level based on errors.length"
      pattern: "hasErrors.*logger\\.warn|logger\\.info"
    - from: "comprobantes-cabecera-client.ts"
      to: "logger"
      via: "conditional log level based on errors.length"
      pattern: "hasErrors.*logger\\.warn|logger\\.info"
---

<objective>
Fix the 207 Multi-Status bug in all 4 sync API clients so that batches with HTTP 207 and 0 errors count as successful instead of failures.

Purpose: Currently all 4 sync clients return `success: false` for ANY 207 response, even when `errors.length === 0`. This causes correct batches to be counted as failures in sync metrics (FIX-01, per decisions D-05 and D-06).
Output: All 4 API clients correctly return `success: true` for 207 responses with 0 errors and log at `info` level.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-script-adaptation-207-fix/25-CONTEXT.md
@.planning/phases/25-script-adaptation-207-fix/25-RESEARCH.md

<interfaces>
<!-- Current 207 handling patterns in each client — executor must preserve result extraction differences -->

From objetiva-sync/src/api-client/articulos-client.ts (line 135):
```typescript
const result = data.data || data.result;  // Uses data.result as fallback
```

From objetiva-sync/src/api-client/comprobantes-cabecera-client.ts (line 118):
```typescript
const result = data.data || data;  // Uses data as fallback (NOT data.result)
```

From objetiva-sync/src/api-client/comprobantes-detalle-client.ts (line 153):
```typescript
const result = data.data || data;  // Uses data as fallback
```

From objetiva-sync/src/api-client/comprobantes-pagos-client.ts (line 116):
```typescript
const result = data.data || data;  // Uses data as fallback
```

IMPORTANT: articulos uses `data.result` fallback, the other 3 use `data` fallback. Preserve each client's existing extraction line exactly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 207 handling in all 4 sync API clients</name>
  <files>
    objetiva-sync/src/api-client/articulos-client.ts,
    objetiva-sync/src/api-client/comprobantes-cabecera-client.ts,
    objetiva-sync/src/api-client/comprobantes-detalle-client.ts,
    objetiva-sync/src/api-client/comprobantes-pagos-client.ts
  </files>
  <read_first>
    objetiva-sync/src/api-client/articulos-client.ts
    objetiva-sync/src/api-client/comprobantes-cabecera-client.ts
    objetiva-sync/src/api-client/comprobantes-detalle-client.ts
    objetiva-sync/src/api-client/comprobantes-pagos-client.ts
  </read_first>
  <action>
**Per D-05 and D-06 — fix in sync clients only, conditional on errors.length:**

Apply the following transformation to the `if (response.status === 207)` block in each of the 4 files. Each client's result extraction line (`data.data || data.result` vs `data.data || data`) MUST be preserved as-is.

**For articulos-client.ts** (lines ~133-153), replace the 207 block with:

```typescript
// Manejo de 207 Multi-Status
if (response.status === 207) {
  const result = data.data || data.result;  // KEEP: articulos-specific fallback

  if (!result) {
    throw new Error('No data in 207 Multi-Status response');
  }

  const errors = result.errors || [];
  const hasErrors = errors.length > 0;

  if (hasErrors) {
    logger.warn({
      inserted: result.inserted || 0,
      updated: result.updated || 0,
      errors: errors.length,
    }, '[ArticulosClient] ⚠️ Batch con éxito parcial (207 Multi-Status)');
  } else {
    logger.info({
      inserted: result.inserted || 0,
      updated: result.updated || 0,
    }, '[ArticulosClient] Batch exitoso, sin errores');
  }

  return {
    success: !hasErrors,
    inserted: result.inserted || 0,
    updated: result.updated || 0,
    errors,
  };
}
```

**For comprobantes-cabecera-client.ts** (lines ~116-137), replace with same pattern but:
- Result extraction: `const result = data.data || data;`  (NOT data.result)
- Logger prefix: `[ComprobantesClient]` (matches existing prefix in file)

**For comprobantes-detalle-client.ts** (lines ~151-172), replace with same pattern but:
- Result extraction: `const result = data.data || data;`
- Logger prefix: `[ComprobantesDetalleClient]` (matches existing prefix in file)

**For comprobantes-pagos-client.ts** (lines ~114-135), replace with same pattern but:
- Result extraction: `const result = data.data || data;`
- Logger prefix: `[ComprobantesPagosClient]` (matches existing prefix in file)

**Changes in each file (identical logic, different prefixes and result extraction):**
1. Add `const errors = result.errors || [];` and `const hasErrors = errors.length > 0;`
2. Replace single `logger.warn(...)` with `if (hasErrors) { logger.warn(...) } else { logger.info(...) }`
3. Change `success: false` to `success: !hasErrors`
4. Change `errors: result.errors || []` to `errors` (use the already-computed variable)
5. Info log message: `'[XxxClient] Batch exitoso, sin errores'` (no emoji, per D-06)
6. Warn log keeps existing emoji: `'[XxxClient] ⚠️ Batch con éxito parcial (207 Multi-Status)'`
  </action>
  <verify>
    <automated>cd "C:/Users/sistemas/.proyectos/objetiva-sync-monorepo" && grep -c "success: !hasErrors" objetiva-sync/src/api-client/articulos-client.ts objetiva-sync/src/api-client/comprobantes-cabecera-client.ts objetiva-sync/src/api-client/comprobantes-detalle-client.ts objetiva-sync/src/api-client/comprobantes-pagos-client.ts && grep -c "Batch exitoso, sin errores" objetiva-sync/src/api-client/articulos-client.ts objetiva-sync/src/api-client/comprobantes-cabecera-client.ts objetiva-sync/src/api-client/comprobantes-detalle-client.ts objetiva-sync/src/api-client/comprobantes-pagos-client.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `articulos-client.ts` contains `success: !hasErrors` (not `success: false`)
    - `comprobantes-cabecera-client.ts` contains `success: !hasErrors`
    - `comprobantes-detalle-client.ts` contains `success: !hasErrors`
    - `comprobantes-pagos-client.ts` contains `success: !hasErrors`
    - `articulos-client.ts` contains `const errors = result.errors || []`
    - `articulos-client.ts` contains `const hasErrors = errors.length > 0`
    - All 4 files contain `logger.info(` with `Batch exitoso, sin errores`
    - All 4 files contain `logger.warn(` with `Batch con éxito parcial (207 Multi-Status)`
    - `articulos-client.ts` preserves `data.data || data.result` (NOT `data.data || data`)
    - `comprobantes-cabecera-client.ts` preserves `data.data || data` (NOT `data.data || data.result`)
    - `comprobantes-detalle-client.ts` preserves `data.data || data`
    - `comprobantes-pagos-client.ts` preserves `data.data || data`
    - No file contains `success: false` in the 207 block (old bug pattern removed)
  </acceptance_criteria>
  <done>All 4 sync API clients return success: true for 207 with 0 errors and success: false for 207 with errors > 0. Log levels are info for success and warn for partial success. Each client preserves its original result extraction pattern.</done>
</task>

</tasks>

<verification>
1. `grep "success: !hasErrors" objetiva-sync/src/api-client/*.ts | wc -l` — returns 4
2. `grep "Batch exitoso, sin errores" objetiva-sync/src/api-client/*.ts | wc -l` — returns 4
3. `grep "logger.info" objetiva-sync/src/api-client/articulos-client.ts` — has info-level log
4. `grep "data.result" objetiva-sync/src/api-client/articulos-client.ts` — preserves articulos-specific extraction
5. `grep -c "success: false" objetiva-sync/src/api-client/articulos-client.ts` — 0 matches in 207 block
</verification>

<success_criteria>
- All 4 sync clients return success: true when HTTP 207 has errors.length === 0
- All 4 sync clients return success: false when HTTP 207 has errors.length > 0
- Log level is info for 0-error case, warn for errors > 0 case
- Each client's result extraction pattern (data.result vs data fallback) is preserved
- No other behavior in the clients is changed
</success_criteria>

<output>
After completion, create `.planning/phases/25-script-adaptation-207-fix/25-02-SUMMARY.md`
</output>
