---
phase: 05-integration-testing-and-hardening
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - objetiva-sync/tests/integration/schema-validation.integration.test.ts
  - objetiva-sync/tests/integration/validation-errors.integration.test.ts
  - objetiva-sync/tests/helpers/schema-test-utils.ts
autonomous: true

must_haves:
  truths:
    - "Developer is alerted when query fields don't match the expected schema"
    - "Validation errors guide developer to fix queries with specific field names and suggestions"
    - "Developer can verify current schemas are in sync before deployment"
    - "Schema changes propagate from database through CLI to validation layer"
  artifacts:
    - path: "objetiva-sync/tests/integration/schema-validation.integration.test.ts"
      provides: "Schema validation and propagation test"
      contains: "describe.*Schema.*Validation"
    - path: "objetiva-sync/tests/integration/validation-errors.integration.test.ts"
      provides: "Validation error reporting test"
      contains: "describe.*Validation.*Error"
    - path: "objetiva-sync/tests/helpers/schema-test-utils.ts"
      provides: "Schema testing utilities"
      exports: ["verifySchemaFile", "getSchemaFields"]
  key_links:
    - from: "objetiva-sync/tests/integration/schema-validation.integration.test.ts"
      to: "objetiva-sync/src/sync/schema-validator.ts"
      via: "SchemaValidator import"
      pattern: "import.*SchemaValidator|validateQuery"
    - from: "objetiva-sync/tests/integration/validation-errors.integration.test.ts"
      to: "objetiva-sync/src/sync/schema-validator.ts"
      via: "SchemaValidator import"
      pattern: "import.*SchemaValidator|validateQuery"
    - from: "objetiva-sync/tests/integration/schema-validation.integration.test.ts"
      to: "objetiva-sync-gateway/src/cli/regenerate-schemas.ts"
      via: "CLI execution for schema propagation test"
      pattern: "npm run regenerate-schemas|execSync.*regenerate"
---

<objective>
Create tests for schema validation, validation error reporting, AND schema change propagation.

Purpose: Validate that query field mismatches are detected (TEST-05 validation), that validation errors include actionable field-level details (TEST-06), and that schema changes propagate correctly through the full CLI roundtrip (TEST-05 propagation): ALTER TABLE -> CLI regenerate -> Prisma/Zod update -> validation uses new schema.

Output: 2 integration test files + schema testing utilities
</objective>

<execution_context>
@C:\Users\Administrador\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Administrador\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-integration-testing-and-hardening/05-CONTEXT.md
@.planning/phases/05-integration-testing-and-hardening/05-RESEARCH.md
@.planning/phases/05-integration-testing-and-hardening/05-01-SUMMARY.md

# Schema validation and CLI sources
@objetiva-sync/src/sync/schema-validator.ts
@objetiva-sync/src/services/schema-cache-service.ts
@objetiva-sync-gateway/src/cli/regenerate-schemas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schema testing utilities and schema validation test (including propagation)</name>
  <files>
    objetiva-sync/tests/helpers/schema-test-utils.ts
    objetiva-sync/tests/integration/schema-validation.integration.test.ts
  </files>
  <action>
Create `tests/helpers/schema-test-utils.ts`:

1. Export utility functions for schema inspection:
   ```typescript
   export function getSchemaFields(schemaPath: string): string[]
   // Reads schema file, extracts field names

   export function verifySchemaFile(schemaPath: string, expectedFields: string[]): boolean
   // Verifies schema contains expected fields

   export function getPrismaModelFields(modelName: string): string[]
   // Parses schema.prisma to extract model fields

   export function getZodSchemaFields(schemaPath: string): string[]
   // Parses Zod schema file to extract field names
   ```

2. Handle both Prisma (.prisma) and Zod (.ts) schema file parsing

Create `tests/integration/schema-validation.integration.test.ts` (TEST-05):

This file tests BOTH schema validation AND schema change propagation.

**Part A: Schema Validation Tests (test cases 1-5)**

Test cases:

1. `it('should detect missing required fields in query config')`:
   - Create query config missing 'erp_codigo' (required field)
   - Run SchemaValidator.validateQuery()
   - Assert error returned with field name and 'missing_required' type

2. `it('should detect extra fields not in schema')`:
   - Create query config with 'nonexistent_field'
   - Run SchemaValidator.validateQuery()
   - Assert error returned with 'unexpected_field' type

3. `it('should pass validation when all fields match schema')`:
   - Create query config with all correct fields
   - Run SchemaValidator.validateQuery()
   - Assert validation passes with no errors

4. `it('should verify CLI dry-run executes without errors')`:
   - Execute `npm run regenerate-schemas -- --dry-run` in gateway
   - Verify exit code 0
   - Verify output shows expected dry-run behavior

5. `it('should verify current Prisma and Zod schemas are in sync')`:
   - Read Prisma model fields for 'articulo'
   - Read Zod schema fields for articulos
   - Assert both have matching field names

**Part B: Schema Change Propagation Test (test case 6)**

6. `it('should propagate schema changes from database through CLI to validation')`:

   **Prerequisite:** Requires TEST_DATABASE_URL environment variable pointing to a PostgreSQL test database. Skip test if not available (allows CI without full database).

   Test the full roundtrip as required by CONTEXT.md:

   a. **Setup:** Connect to test database using TEST_DATABASE_URL

   b. **ALTER TABLE:** Execute `ALTER TABLE articulos ADD COLUMN test_propagation_column TEXT`

   c. **CLI Regenerate:** Run `npm run regenerate-schemas` in gateway directory (NOT dry-run)
      - Use execSync with cwd set to gateway directory
      - Capture stdout/stderr for debugging

   d. **Verify Prisma Schema:** Read `objetiva-sync-gateway/prisma/schema.prisma`
      - Use getPrismaModelFields('articulo')
      - Assert 'test_propagation_column' is present in fields

   e. **Verify Zod Schema:** Read `objetiva-sync/src/types/articulos.ts` (or wherever Zod schemas live)
      - Use getZodSchemaFields()
      - Assert 'test_propagation_column' is present

   f. **Verify Validation Uses New Schema:**
      - Clear schema cache (SchemaValidator.clearCache() or similar)
      - Create a query config that references 'test_propagation_column'
      - Run SchemaValidator.validateQuery()
      - Assert validation PASSES (field is recognized)
      - Create a query config that OMITS 'test_propagation_column' when it should be required
      - Run SchemaValidator.validateQuery()
      - Assert validation correctly handles the new field

   g. **Cleanup:** Execute `ALTER TABLE articulos DROP COLUMN test_propagation_column`
      - Run regenerate-schemas again to restore original state
      - Use afterEach/finally to ensure cleanup even on test failure

   **Environment check at top of describe block:**
   ```typescript
   const TEST_DATABASE_URL = process.env.TEST_DATABASE_URL;
   const skipPropagation = !TEST_DATABASE_URL;

   (skipPropagation ? describe.skip : describe)('Schema Propagation', () => {
     // Test case 6 lives here
   });
   ```
  </action>
  <verify>
    `cd objetiva-sync && npm test -- tests/integration/schema-validation.integration.test.ts`

    Without TEST_DATABASE_URL: 5 tests pass, 1 test skipped (propagation)
    With TEST_DATABASE_URL: All 6 tests pass
  </verify>
  <done>
    - schema-test-utils.ts exports utility functions for Prisma and Zod parsing
    - schema-validation.integration.test.ts has 5 validation tests + 1 propagation test
    - Tests 1-5 validate field mismatch detection (no database required)
    - Test 6 validates full ALTER TABLE -> CLI -> Prisma/Zod -> Validation flow
    - Test 6 skips gracefully when TEST_DATABASE_URL not set
    - CLI dry-run verification works
  </done>
</task>

<task type="auto">
  <name>Task 2: Create validation error reporting tests</name>
  <files>
    objetiva-sync/tests/integration/validation-errors.integration.test.ts
  </files>
  <action>
Create `tests/integration/validation-errors.integration.test.ts` (TEST-06):

Test the SchemaValidator from Phase 4 produces properly formatted errors.

Test cases:

1. `it('should report missing required fields')`:
   - Create query config missing required field (e.g., 'erp_codigo')
   - Validate against schema
   - Assert error includes:
     - Field name that's missing
     - Error type: 'missing_required'
     - Severity: 'error'

2. `it('should report unexpected extra fields')`:
   - Create query config with extra field not in schema (e.g., 'nonexistent_field')
   - Validate against schema
   - Assert error includes:
     - Field name: 'nonexistent_field'
     - Error type: 'unexpected_field'
     - Suggested alternatives (if typo detected)

3. `it('should suggest similar field names for typos')`:
   - Create query with 'sku_codigo' (typo of 'sku' or 'erp_codigo')
   - Validate against schema
   - Assert suggestion includes correct field name
   - Verify Levenshtein distance <= 3 triggers suggestion

4. `it('should report type mismatches')`:
   - Create mapping where source is number but target expects string
   - Validate
   - Assert error includes type mismatch details

5. `it('should format errors for dashboard display')`:
   - Generate validation errors
   - Verify error structure matches dashboard expectations:
     ```typescript
     {
       field: string,
       error: string,
       type: 'missing' | 'extra' | 'type_mismatch' | 'typo',
       suggestion?: string
     }
     ```

Mock the schema cache service to return known schema for testing (don't require gateway to be running).
  </action>
  <verify>
    `cd objetiva-sync && npm test -- tests/integration/validation-errors.integration.test.ts`
    All 5 validation error tests pass.
  </verify>
  <done>
    - validation-errors.integration.test.ts has 5 passing tests
    - Tests verify field-level error detail
    - Tests verify suggestion mechanism for typos
    - Error format matches dashboard display requirements
  </done>
</task>

</tasks>

<verification>
1. `cd objetiva-sync && npm test -- tests/integration/schema-validation` - Schema validation tests pass
2. `cd objetiva-sync && npm test -- tests/integration/validation-errors` - Validation tests pass
3. Schema utilities properly parse Prisma and Zod files
4. Validation errors include actionable suggestions
5. With TEST_DATABASE_URL: Full propagation test passes (ALTER TABLE -> CLI -> validation)
</verification>

<success_criteria>
- TEST-05 validation: Schema validation test detects field mismatches between queries and schemas
- TEST-05 propagation: Schema change propagation test validates ALTER TABLE -> CLI regenerate -> Prisma/Zod update -> validation uses new schema
- TEST-06: Validation error tests verify field-level detail and suggestions
- All tests pass in under 30 seconds (propagation test may take longer due to CLI execution)
- Tests don't require external services for basic validation cases (propagation test requires TEST_DATABASE_URL)
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-testing-and-hardening/05-02-SUMMARY.md`
</output>
