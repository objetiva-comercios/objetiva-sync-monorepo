---
phase: 12-end-to-end-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - objetiva-sync/tests/integration/12-01-workflow-validation.test.ts
autonomous: true

must_haves:
  truths:
    - "Sync client correctly handles all gateway response types (200, 207, 401, 500) and classifies errors appropriately"
    - "Zod validation accepts data matching the expected schema for all 4 entity types and rejects malformed data"
    - "Error classifier correctly categorizes all 11 error types with correct codes and isRetryable flags"
    - "Full send-validate flow works end-to-end: fixtures pass Zod, send via mocked API client, receive correct confirmation"
  artifacts:
    - path: "objetiva-sync/tests/integration/12-01-workflow-validation.test.ts"
      provides: "Integration tests for full workflow validation"
      contains: "describe.*Workflow Validation"
  key_links:
    - from: "objetiva-sync/tests/integration/12-01-workflow-validation.test.ts"
      to: "objetiva-sync/tests/helpers/gateway-mock.ts"
      via: "imports createMockApiClient, createMockApiClientWithErrors, createMockApiClientWithPartialSuccess"
      pattern: "createMockApiClient"
    - from: "objetiva-sync/tests/integration/12-01-workflow-validation.test.ts"
      to: "objetiva-sync/tests/fixtures/test-data.ts"
      via: "imports createArticulo, createArticuloBatch, createComprobanteCabecera, createComprobanteDetalle, createComprobantePago"
      pattern: "createArticulo|createComprobanteCabecera"
    - from: "objetiva-sync/tests/integration/12-01-workflow-validation.test.ts"
      to: "objetiva-sync/src/utils/error-classifier.ts"
      via: "imports and validates classifyError for all error types"
      pattern: "classifyError"
---

<objective>
Create end-to-end workflow validation integration tests for the sync pipeline, reusing existing test infrastructure.

Purpose: Validates ROBU-01 -- that the complete sync workflow (validation and data sync) works correctly. This is the foundation test plan that proves the happy-path pipeline works, Zod schemas are correct, and error classification is complete.

Output: A comprehensive workflow validation test suite covering: API client batch sending with mocked API client, Zod schema validation for all 4 entity types, error classifier completeness, and the full send-validate-persist flow.

IMPORTANT: This plan reuses existing test infrastructure -- do NOT create duplicate helpers:
- `tests/helpers/gateway-mock.ts` already provides `createMockApiClient()`, `createMockApiClientWithErrors()`, `createMockApiClientWithPartialSuccess()` using Vitest vi.fn() mocks
- `tests/fixtures/test-data.ts` already provides `createArticulo()`, `createArticuloBatch()`, `createComprobanteCabecera()`, `createComprobanteCabeceraBatch()`, `createComprobanteDetalle()`, `createComprobantePago()`, `resetCounter()`
</objective>

<execution_context>
@C:\Users\Administrador\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Administrador\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-end-to-end-robustness/12-RESEARCH.md
@.planning/codebase/TESTING.md

# Key source files to understand existing patterns
@objetiva-sync/vitest.config.ts
@objetiva-sync/src/api-client/articulos-client.ts
@objetiva-sync/src/api-client/auth.ts
@objetiva-sync/src/utils/error-classifier.ts
@objetiva-sync/src/types/articulos.ts
@objetiva-sync/src/types/comprobantes-cabecera.ts
@objetiva-sync/src/types/comprobantes-detalle.ts
@objetiva-sync/src/types/comprobantes-pagos.ts
@objetiva-sync/package.json
@objetiva-sync/tests/setup.ts

# EXISTING test infrastructure -- REUSE these, do NOT recreate
@objetiva-sync/tests/helpers/gateway-mock.ts
@objetiva-sync/tests/fixtures/test-data.ts

# Existing integration tests for pattern reference
@objetiva-sync/tests/integration/articulos.integration.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write workflow validation integration tests</name>
  <files>
    objetiva-sync/tests/integration/12-01-workflow-validation.test.ts
  </files>
  <action>
    Create `tests/integration/12-01-workflow-validation.test.ts` with these test groups.

    CRITICAL: REUSE existing test infrastructure. Do NOT install nock. Do NOT create new gateway-mock or fixture files. Import from:
    - `import { createMockApiClient, createMockApiClientWithErrors, createMockApiClientWithPartialSuccess } from '../helpers/gateway-mock.js';`
    - `import { createArticulo, createArticuloBatch, createComprobanteCabecera, createComprobanteCabeceraBatch, createComprobanteDetalle, createComprobantePago, resetCounter } from '../fixtures/test-data.js';`
    - `import { classifyError } from '../../src/utils/error-classifier.js';`

    Follow the same patterns as `tests/integration/articulos.integration.test.ts` (which already uses these helpers).

    **Group 1: "Zod Schema Validation for All Entity Types"**
    Import actual Zod schemas from `src/types/*.ts` files (e.g., `articuloPayloadSchema`).
    - Test: "valid articulo fixture passes Zod validation" -- parse createArticulo() through articuloPayloadSchema, expect success
    - Test: "valid cabecera fixture passes Zod validation" -- same for comprobantes_cabecera
    - Test: "valid detalle fixture passes Zod validation" -- create a cabecera first, then use createComprobanteDetalle(cabecera) and parse through schema
    - Test: "valid pago fixture passes Zod validation" -- same pattern with createComprobantePago(cabecera)
    - Test: "invalid articulo is rejected by Zod" -- pass object with missing required fields, expect safeParse failure
    - Test: "Zod transforms types correctly" -- verify numeric string -> number coercion if schemas have transforms

    **Group 2: "API Client Mock Sends Batch Successfully"**
    Use the existing `createMockApiClient()` from `tests/helpers/gateway-mock.ts` which provides a fully mocked APIClient with vi.fn() methods.
    - Test: "mock API client sends articulos batch and receives success response" --
      1. Create mockApiClient via createMockApiClient()
      2. Create 10 articulos via createArticuloBatch(10)
      3. Call mockApiClient.articulos.sendBatch(batch)
      4. Assert result.success === true, result.inserted === 10
    - Test: "mock API client with errors returns error results" --
      1. Create mockApiClient via createMockApiClientWithErrors()
      2. Call mockApiClient.articulos.sendBatch(batch)
      3. Assert result.success === false, result.errors.length > 0
    - Test: "mock API client with partial success returns mixed results" --
      1. Create mockApiClient via createMockApiClientWithPartialSuccess()
      2. Call sendBatch with 10 items
      3. Assert result.inserted > 0 AND result.errors.length > 0

    **Group 3: "Error Classifier Completeness"**
    Import `classifyError` from `src/utils/error-classifier.ts`. Test all 11 error types:
    - Test: "classifies AbortError as SYNC_CANCELED" -- new DOMException('', 'AbortError')
    - Test: "classifies TimeoutError as TIMEOUT_GATEWAY_REQUEST"
    - Test: "classifies HeadersTimeoutError as TIMEOUT_GATEWAY_HEADERS"
    - Test: "classifies BodyTimeoutError as TIMEOUT_GATEWAY_BODY"
    - Test: "classifies ETIMEOUT as TIMEOUT_ERP_QUERY"
    - Test: "classifies ECONNREFUSED as GATEWAY_UNREACHABLE with isRetryable=true"
    - Test: "classifies ECONNRESET as GATEWAY_CONNECTION_RESET with isRetryable=true"
    - Test: "classifies ENOTFOUND as GATEWAY_DNS_ERROR with isRetryable=false"
    - Test: "classifies HTTP 401 as GATEWAY_AUTH_ERROR with isRetryable=false"
    - Test: "classifies HTTP 500 as GATEWAY_SERVER_ERROR with isRetryable=true"
    - Test: "classifies unknown error as UNKNOWN_ERROR"
    - For each test: verify code, isRetryable, and that message is non-empty

    **Group 4: "Full Send-Validate-Persist Flow"**
    - Test: "complete articulos flow: create fixtures -> validate -> send -> receive confirmation"
      1. Create 10 articulo fixtures via createArticuloBatch(10)
      2. Validate each passes Zod schema (articuloPayloadSchema.safeParse)
      3. Send batch via createMockApiClient().articulos.sendBatch(batch)
      4. Assert result.success === true, result.inserted === 10
    - Test: "batch with mixed valid/invalid data flow"
      1. Create 8 valid articulos via createArticuloBatch(8)
      2. Verify all 8 pass Zod validation
      3. Create 2 invalid articulos (missing required fields), verify they FAIL Zod
      4. Send only valid ones via mock API client
      5. Assert result.inserted === 8

    Use beforeEach to call resetCounter() for test isolation.
    Mock the logger to suppress output: `vi.mock('../../src/utils/logger.js')`.
    Follow project conventions: describe/it structure, async/await.
    Set testTimeout appropriately (30000ms for integration tests).
  </action>
  <verify>
    1. `cd objetiva-sync && npx vitest run tests/integration/12-01-workflow-validation.test.ts` -- all tests pass
    2. Test count: at minimum 15 tests across 4 groups
    3. No test warnings or unhandled promise rejections
    4. `cd objetiva-sync && npx vitest run` -- existing tests still pass (no regressions)
  </verify>
  <done>
    All workflow validation tests pass. Zod schemas validated for all 4 entity types using existing test-data.ts fixtures. API client correctly tested via existing gateway-mock.ts helpers (success, error, partial success). All 11 error classifier types verified with correct codes and isRetryable flags. Full send-validate flow confirmed working.
  </done>
</task>

</tasks>

<verification>
1. `cd objetiva-sync && npx vitest run tests/integration/12-01-workflow-validation.test.ts` -- all tests pass (0 failures)
2. `cd objetiva-sync && npx vitest run` -- existing tests still pass (no regressions)
3. Test output shows all 4 groups executing with expected test count
4. No new dependencies installed (reuses existing helpers and fixtures)
</verification>

<success_criteria>
- All 4 entity type Zod schemas validated with existing fixtures from tests/fixtures/test-data.ts
- API client batch sending tested using existing mock from tests/helpers/gateway-mock.ts (success, error, partial success)
- All 11 error classifier types have dedicated tests
- Full send-validate flow tested end-to-end with mocked API client
- All tests pass with `npx vitest run`
- No new test helper files or dependencies created (reused existing infrastructure)
</success_criteria>

<output>
After completion, create `.planning/phases/12-end-to-end-robustness/12-01-SUMMARY.md`
</output>
