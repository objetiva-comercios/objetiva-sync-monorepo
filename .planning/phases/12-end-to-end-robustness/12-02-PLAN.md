---
phase: 12-end-to-end-robustness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - objetiva-sync/tests/integration/12-02-error-recovery.test.ts
  - objetiva-sync/tests/integration/12-02-data-integrity.test.ts
autonomous: true

must_haves:
  truths:
    - "When gateway is temporarily unreachable, sync client retries and recovers when connection restores"
    - "When a batch fails mid-sync, the batch processor retries with exponential backoff and continues processing remaining batches"
    - "Error recovery does not produce duplicate records -- batch retry with same data results in same record count"
    - "User cancellation (AbortSignal) stops sync immediately without corrupting state"
    - "RetryQueueManager correctly adds failed batches and processes them on retry"
  artifacts:
    - path: "objetiva-sync/tests/integration/12-02-error-recovery.test.ts"
      provides: "Error recovery and retry tests using mocked API client and batch processor"
      contains: "describe.*Error Recovery"
    - path: "objetiva-sync/tests/integration/12-02-data-integrity.test.ts"
      provides: "Data integrity tests validating no duplicates on retry and state consistency"
      contains: "describe.*Data Integrity"
  key_links:
    - from: "objetiva-sync/tests/integration/12-02-error-recovery.test.ts"
      to: "objetiva-sync/src/sync/batch-processor.ts"
      via: "tests processBatchWithRetry and processBatches with simulated failures"
      pattern: "processBatchWithRetry|processBatches"
    - from: "objetiva-sync/tests/integration/12-02-error-recovery.test.ts"
      to: "objetiva-sync/src/sync/sync-state-manager.ts"
      via: "mocks syncStateManager.getCurrentSync() via vi.mock to control cancellation behavior"
      pattern: "syncStateManager|getCurrentSync"
    - from: "objetiva-sync/tests/integration/12-02-error-recovery.test.ts"
      to: "objetiva-sync/tests/helpers/gateway-mock.ts"
      via: "imports createMockApiClient, createMockApiClientWithErrors for API client tests"
      pattern: "createMockApiClient"
    - from: "objetiva-sync/tests/integration/12-02-data-integrity.test.ts"
      to: "objetiva-sync/src/sync/batch-processor.ts"
      via: "validates no duplicate processing via batch confirmation policy, calculateBatchStats, mergeBatchResults"
      pattern: "isBatchConfirmed|processBatches|calculateBatchStats|mergeBatchResults"
    - from: "objetiva-sync/tests/integration/12-02-error-recovery.test.ts"
      to: "objetiva-sync/src/sync/retry-queue-manager.ts"
      via: "tests addFailedBatch and processRetries lifecycle"
      pattern: "RetryQueueManager"
---

<objective>
Create error recovery and data integrity integration tests that validate the sync pipeline's resilience under failure conditions.

Purpose: Validates ROBU-02 -- that error recovery works across the pipeline (retry, graceful degradation) and that recovery does not produce duplicate records or corrupt data. This plan proves the system is robust under adverse conditions.

Output: Two test files covering: gateway unreachable recovery with retry, batch failure with exponential backoff, AbortSignal cancellation handling, RetryQueueManager lifecycle, and data integrity validation (no duplicates on retry, state consistency after failure).

IMPORTANT: This plan reuses existing test infrastructure -- do NOT create duplicate helpers:
- `tests/helpers/gateway-mock.ts` already provides `createMockApiClient()`, `createMockApiClientWithErrors()`, `createMockApiClientWithPartialSuccess()`
- `tests/fixtures/test-data.ts` already provides all fixture factory functions
- `tests/helpers/test-db.ts` already provides `createTestDb()`, `clearTestDb()`, `closeTestDb()`
- `tests/helpers/db-injector.ts` already provides `injectTestDatabase()`
</objective>

<execution_context>
@C:\Users\Administrador\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Administrador\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-end-to-end-robustness/12-RESEARCH.md
@.planning/codebase/TESTING.md

# Key source files for error recovery and retry logic
@objetiva-sync/src/sync/batch-processor.ts
@objetiva-sync/src/sync/retry-queue-manager.ts
@objetiva-sync/src/utils/error-classifier.ts
@objetiva-sync/src/api-client/articulos-client.ts
@objetiva-sync/src/api-client/auth.ts
@objetiva-sync/src/sync/sync-state-manager.ts
@objetiva-sync/src/store/repositories/retry-queue-repo.ts
@objetiva-sync/src/store/schema.ts
@objetiva-sync/src/types/common.ts

# EXISTING test infrastructure -- REUSE these, do NOT recreate
@objetiva-sync/tests/helpers/gateway-mock.ts
@objetiva-sync/tests/fixtures/test-data.ts
@objetiva-sync/tests/helpers/test-db.ts
@objetiva-sync/tests/helpers/db-injector.ts
@objetiva-sync/tests/setup.ts

# Existing integration tests for pattern reference
@objetiva-sync/tests/integration/articulos.integration.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write error recovery and retry integration tests</name>
  <files>
    objetiva-sync/tests/integration/12-02-error-recovery.test.ts
  </files>
  <action>
    Create `tests/integration/12-02-error-recovery.test.ts` with these test groups.

    CRITICAL: REUSE existing test infrastructure. Import from:
    - `import { createMockApiClient, createMockApiClientWithErrors } from '../helpers/gateway-mock.js';`
    - `import { createArticuloBatch, resetCounter } from '../fixtures/test-data.js';`
    - `import { createTestDb, clearTestDb, closeTestDb } from '../helpers/test-db.js';`
    - `import { injectTestDatabase } from '../helpers/db-injector.js';`

    **syncStateManager mocking setup (CRITICAL for batch processor tests):**
    The batch processor imports `syncStateManager` singleton from `'./sync-state-manager.js'` and calls `syncStateManager.getCurrentSync()` to check for cancellation at multiple checkpoints (lines 172, 329, 413, 477 of batch-processor.ts).

    Mock it at the module level:
    ```typescript
    vi.mock('../../src/sync/sync-state-manager.js', () => ({
      syncStateManager: {
        getCurrentSync: vi.fn().mockReturnValue({ status: 'running' }),
        startSync: vi.fn(),
        updateProgress: vi.fn(),
        completeSync: vi.fn(),
        isSyncing: vi.fn().mockReturnValue(false),
        cancelSync: vi.fn(),
        reset: vi.fn(),
        getHistory: vi.fn().mockReturnValue([]),
      }
    }));
    ```
    Then import it for per-test configuration:
    ```typescript
    import { syncStateManager } from '../../src/sync/sync-state-manager.js';
    ```
    In `beforeEach`, reset to default: `vi.mocked(syncStateManager.getCurrentSync).mockReturnValue({ status: 'running' } as any);`
    For cancellation tests: `vi.mocked(syncStateManager.getCurrentSync).mockReturnValueOnce({ status: 'running' } as any).mockReturnValue({ status: 'canceled' } as any);`

    Also mock logger: `vi.mock('../../src/utils/logger.js');`
    Also mock batch-storage to prevent file I/O: `vi.mock('../../src/utils/batch-storage.js', () => ({ saveBatch: vi.fn() }));`

    **Group 1: "Gateway Unreachable Recovery" (4 tests)**
    Uses existing `createMockApiClient()` and `createMockApiClientWithErrors()` from `tests/helpers/gateway-mock.ts`.
    Note: These mock the APIClient interface directly (not HTTP). For gateway error scenarios, configure the mock's sendBatch to throw or reject.

    - Test: "API client returns error when gateway sendBatch fails (ECONNREFUSED)" --
      1. Create mock client, override articulos.sendBatch to reject with Error containing 'ECONNREFUSED')
      2. Catch error, classify via classifyError
      3. Assert classified.code === 'GATEWAY_UNREACHABLE', classified.isRetryable === true

    - Test: "API client recovers when gateway comes back online" --
      1. Create mock client, sendBatch: first call rejects (ECONNREFUSED), second call resolves success
      2. First call: catch, assert failure
      3. Second call: assert success with inserted count

    - Test: "API client handles HTTP 500 from gateway" --
      1. Create mock client, sendBatch returns { success: false, inserted: 0, updated: 0, errors: [{ code: 'GATEWAY_SERVER_ERROR' }] }
      2. Assert result.success === false
      3. Classify the error, assert GATEWAY_SERVER_ERROR with isRetryable=true

    - Test: "API client handles HTTP 401 (auth failure)" --
      1. Create mock client, sendBatch rejects with Error('HTTP 401: Unauthorized')
      2. Classify the error, assert GATEWAY_AUTH_ERROR with isRetryable=false

    **Group 2: "Batch Processor Retry with Exponential Backoff" (6 tests)**
    Import `processBatchWithRetry` and `processBatches` from `src/sync/batch-processor.ts`.
    Use `vi.useFakeTimers()` to avoid real delays during backoff sleeps.

    - Test: "processBatchWithRetry retries on failure and succeeds on second attempt" --
      1. Create mock processFn: attempt 1 returns { success: false, inserted: 0, updated: 0, errors: [{ index: 0, identifier: 'X', error: 'fail', code: 'ERR' }] }, attempt 2 returns { success: true, inserted: 10, updated: 0, errors: [] }
      2. Call processBatchWithRetry(batch, mockProcessFn, { batchNumber: 1, totalBatches: 1 }, 3)
      3. Advance timers (vi.advanceTimersByTimeAsync) between retries
      4. Assert mockProcessFn called exactly 2 times
      5. Assert final result.success === true

    - Test: "processBatchWithRetry gives up after maxRetries" --
      1. processFn always returns { success: false, inserted: 0, updated: 0, errors: [...] }
      2. maxRetries=3, advance timers between attempts
      3. Assert processFn called exactly 3 times
      4. Assert result.errors[0].code === 'MAX_RETRIES_EXCEEDED' (the fallback at end of function)

    - Test: "processBatchWithRetry does NOT retry on 4xx client error" --
      1. processFn throws Error('HTTP 400: Bad Request')
      2. Call processBatchWithRetry
      3. Assert processFn called exactly 1 time (4xx caught, throws immediately)
      4. Assert error is thrown (not swallowed)

    - Test: "processBatchWithRetry returns partial result without retrying" --
      1. processFn returns { success: false, inserted: 5, updated: 0, errors: [{ index: 0, identifier: 'X', error: 'fail', code: 'ERR' }] }
      2. Call processBatchWithRetry
      3. Assert processFn called exactly 1 time (partial success = no retry)
      4. Assert result.inserted === 5

    - Test: "processBatches continues processing remaining batches when continueOnError=true" --
      1. Create 30 items, batchSize=10 (3 batches)
      2. processFn: batch 1 returns success (10 inserted), batch 2 returns failure (0 inserted, errors), batch 3 returns success (10 inserted)
      3. processBatches with continueOnError=true, enableRetries=false
      4. Assert: successfulBatches=2, failedBatches=1, inserted=20

    - Test: "processBatches stops on first failure when continueOnError=false" --
      1. Same 3 batches, batch 2 fails
      2. processBatches with continueOnError=false, enableRetries=false
      3. Assert: batch 3 NOT processed (only 1+2), failedBatches >= 1

    **Group 3: "Cancellation via AbortSignal" (2 tests)**
    - Test: "processBatchWithRetry aborts on sync cancellation" --
      1. Mock syncStateManager.getCurrentSync():
         - First call returns { status: 'running' } (allow attempt 1)
         - Subsequent calls return { status: 'canceled' }
         Use: `vi.mocked(syncStateManager.getCurrentSync).mockReturnValueOnce({ status: 'running' } as any).mockReturnValue({ status: 'canceled' } as any);`
      2. processFn fails on first attempt (returns total failure)
      3. Call processBatchWithRetry with maxRetries=3
      4. Assert processFn called exactly 1 time (cancellation prevents retry)
      5. Assert result.errors contains SYNC_CANCELED

    - Test: "processBatches stops when sync is canceled between batches" --
      1. Mock syncStateManager.getCurrentSync():
         - Returns { status: 'running' } for batch 1
         - Returns { status: 'canceled' } for batch 2 check
      2. Create 30 items (3 batches)
      3. processBatches with enableRetries=false
      4. Assert: only batch 1 processed
      5. Assert: result.errors contains SYNC_CANCELED

    **Group 4: "RetryQueueManager Lifecycle" (4 tests)**
    Uses test SQLite database (in-memory) from `tests/helpers/test-db.ts`.
    Setup: `const db = createTestDb(); injectTestDatabase(db);`

    - Test: "addFailedBatch creates retry queue entry" --
      1. Initialize test DB + inject
      2. Create RetryQueueManager instance
      3. Call addFailedBatch with entityType='articulos', payload, error
      4. Assert returns item ID > 0

    - Test: "processRetries calls processor function for pending items" --
      1. Add 2 failed batches
      2. Mock processor that returns { success: true }
      3. Call processRetries(mockProcessor)
      4. Assert result: processed=2, successful=2

    - Test: "processRetries handles processor failure and increments attempt count" --
      1. Add 1 failed batch
      2. Mock processor returns { success: false, error: 'Still failing' }
      3. Call processRetries
      4. Assert attempt count incremented

    - Test: "processRetries marks item as permanently failed after max attempts" --
      1. Add 1 failed batch
      2. Process retries maxAttempts times with always-failing processor
      3. Assert item status becomes FAILED permanently

    Use `afterEach(() => { closeTestDb(db); vi.restoreAllMocks(); })` for cleanup.
  </action>
  <verify>
    1. `cd objetiva-sync && npx vitest run tests/integration/12-02-error-recovery.test.ts` -- all tests pass
    2. Test count: at minimum 16 tests across 4 groups
    3. No unhandled promise rejections or test timeouts
  </verify>
  <done>
    All error recovery tests pass. Gateway unreachable/recovery scenarios validated. Batch processor retry with exponential backoff tested (success after retry, max retries exceeded, no retry on 4xx, partial success handling). syncStateManager mocked correctly -- cancellation stops processing immediately. RetryQueueManager lifecycle tested with in-memory test database (add, process, fail permanently).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write data integrity validation tests</name>
  <files>
    objetiva-sync/tests/integration/12-02-data-integrity.test.ts
  </files>
  <action>
    Create `tests/integration/12-02-data-integrity.test.ts` with these test groups.

    CRITICAL: REUSE existing infrastructure. Same imports as Task 1:
    - `import { createMockApiClient, createMockApiClientWithPartialSuccess } from '../helpers/gateway-mock.js';`
    - `import { createArticuloBatch, resetCounter } from '../fixtures/test-data.js';`

    Same syncStateManager mock setup as Task 1 (copy the vi.mock block):
    ```typescript
    vi.mock('../../src/sync/sync-state-manager.js', () => ({
      syncStateManager: {
        getCurrentSync: vi.fn().mockReturnValue({ status: 'running' }),
        startSync: vi.fn(),
        updateProgress: vi.fn(),
        completeSync: vi.fn(),
        isSyncing: vi.fn().mockReturnValue(false),
        cancelSync: vi.fn(),
        reset: vi.fn(),
        getHistory: vi.fn().mockReturnValue([]),
      }
    }));
    import { syncStateManager } from '../../src/sync/sync-state-manager.js';
    ```
    Also mock logger and batch-storage as in Task 1.

    **Group 1: "No Duplicate Records on Batch Retry" (3 tests)**
    - Test: "batch processor tracks inserted vs updated correctly across retries" --
      1. Mock processFn: returns { success: true, inserted: 10, updated: 0, errors: [] }
      2. Call processBatches with 10 items, batchSize=10
      3. Assert result.inserted === 10 (counted only once, not doubled)

    - Test: "upsert response (inserted + updated) correctly reported via mock API client" --
      1. Create mock client, override sendBatch to return { success: true, inserted: 3, updated: 7, errors: [] }
      2. Call sendBatch with 10 items
      3. Assert result.inserted === 3, result.updated === 7

    - Test: "partial batch failure does not inflate record counts" --
      1. Create mock client via createMockApiClientWithPartialSuccess()
      2. Call sendBatch with 10 items
      3. Assert result.inserted === 5 (half), result.errors.length === 5 (other half)
      4. Assert total (inserted + errors) === 10 (no inflation)

    **Group 2: "Sync State Consistency After Failure" (4 tests)**
    - Test: "batch processor result reflects accurate counts after mixed batch outcomes" --
      1. Create 30 items (3 batches of 10)
      2. processFn: batch 1 succeeds (10 inserted), batch 2 fails (0 inserted, errors), batch 3 succeeds (10 inserted)
      3. processBatches with continueOnError=true, enableRetries=false
      4. Assert: result.inserted === 20 (only confirmed batches)
      5. Assert: result.failedBatches === 1, result.successfulBatches === 2

    - Test: "cancellation mid-batch stops processing and reports partial results" --
      1. Mock syncStateManager.getCurrentSync():
         - First call (batch 1 pre-check): { status: 'running' }
         - Second call (batch 1 post-check): { status: 'running' }
         - Third call (batch 2 pre-check): { status: 'canceled' }
      2. Create 30 items (3 batches)
      3. processBatches with enableRetries=false
      4. Assert: only batch 1 processed (inserted=10)
      5. Assert: result.errors contains SYNC_CANCELED
      6. Assert: result.success === false

    - Test: "calculateBatchStats reports correct rates" --
      1. Import calculateBatchStats from batch-processor.ts
      2. Given BatchResult: { success: true, inserted: 8, updated: 2, errors: [{ index: 0, identifier: 'X', error: 'fail', code: 'ERR' }] }
      3. Call calculateBatchStats(result)
      4. Assert: totalProcessed === 11, successRate ~= 90.9, failureRate ~= 9.1

    - Test: "mergeBatchResults correctly combines multiple results" --
      1. Import mergeBatchResults from batch-processor.ts
      2. Create 3 BatchResults: { inserted: 5, updated: 1, errors: [1 error] }, { inserted: 3, updated: 0, errors: [] }, { inserted: 0, updated: 0, errors: [2 errors], success: false }
      3. Call mergeBatchResults(results)
      4. Assert: merged.inserted === 8, merged.updated === 1, merged.errors.length === 3
      5. Assert: merged.success === false (because third result had success=false)

    **Group 3: "Error Classification Drives Retry Decisions" (3 tests)**
    Import `classifyError` from `src/utils/error-classifier.ts`.

    - Test: "retryable errors (ECONNREFUSED, ECONNRESET, HTTP 5xx) are classified as isRetryable=true" --
      1. For each retryable error type: create the error object, classify, assert isRetryable === true
      2. Verify: ECONNREFUSED -> GATEWAY_UNREACHABLE, ECONNRESET -> GATEWAY_CONNECTION_RESET, HTTP 500 -> GATEWAY_SERVER_ERROR

    - Test: "non-retryable errors (ENOTFOUND, HTTP 401, unknown) are classified as isRetryable=false" --
      1. For each non-retryable type: create error, classify, assert isRetryable === false
      2. Note in test comment: batch processor only short-circuits on 4xx, DNS failures exhaust retry attempts

    - Test: "AbortError from user cancellation is classified as SYNC_CANCELED with isRetryable=false" --
      1. Create AbortError: new DOMException('', 'AbortError')
      2. Classify, assert code === 'SYNC_CANCELED', isRetryable === false

    Use beforeEach to call resetCounter() and reset syncStateManager mock.
    Use `vi.useFakeTimers()` if batch processor tests need timer advancement.
  </action>
  <verify>
    1. `cd objetiva-sync && npx vitest run tests/integration/12-02-data-integrity.test.ts` -- all tests pass
    2. Test count: at minimum 10 tests across 3 groups
    3. `cd objetiva-sync && npx vitest run` -- ALL tests in project pass (no regressions)
  </verify>
  <done>
    All data integrity tests pass. Batch retry does not inflate record counts. Upsert responses correctly reported via mock API client. Sync state consistency validated after mixed outcomes and cancellation. calculateBatchStats and mergeBatchResults utility functions verified. Error classification correctly identifies retryable vs non-retryable with correct codes.
  </done>
</task>

</tasks>

<verification>
1. `cd objetiva-sync && npx vitest run tests/integration/12-02-error-recovery.test.ts` -- all tests pass
2. `cd objetiva-sync && npx vitest run tests/integration/12-02-data-integrity.test.ts` -- all tests pass
3. `cd objetiva-sync && npx vitest run` -- full test suite passes (no regressions from new tests)
4. Combined test count for Plan 02: ~26 tests across 7 groups
5. No test flakiness (run twice to confirm stability)
</verification>

<success_criteria>
- Gateway unreachable scenarios tested using existing mock API client helpers
- Batch processor retry validated: success after retry, max retries, no retry on 4xx, partial success, continueOnError behavior
- syncStateManager correctly mocked via vi.mock with explicit getCurrentSync setup
- AbortSignal cancellation tested: immediate stop, no retry after cancel
- RetryQueueManager lifecycle tested with in-memory test database (add, process, fail permanently)
- Data integrity: no duplicates on retry, upsert correctly reported, partial failure accounting
- Sync state consistency: accurate counts after mixed outcomes, cancellation preserves state
- Error classification integration: retryable vs non-retryable drives correct behavior
- All tests pass with `npx vitest run`
- No new test helper files created (reused existing infrastructure)
</success_criteria>

<output>
After completion, create `.planning/phases/12-end-to-end-robustness/12-02-SUMMARY.md`
</output>
