---
phase: 17-dashboard-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/dashboard/package.json
  - shared/dashboard/vite.config.ts
  - shared/dashboard/tsconfig.json
  - shared/dashboard/tsconfig.app.json
  - shared/dashboard/src/index.css
  - shared/dashboard/src/lib/utils.ts
  - shared/dashboard/components.json
autonomous: true

must_haves:
  truths:
    - "shared/dashboard package compiles without errors"
    - "shadcn/ui components can be added via CLI"
    - "Tailwind CSS classes are processed correctly"
  artifacts:
    - path: "shared/dashboard/package.json"
      provides: "React + Vite dashboard package"
      contains: "peerDependencies"
    - path: "shared/dashboard/vite.config.ts"
      provides: "Vite library mode configuration"
      contains: "build.lib"
    - path: "shared/dashboard/components.json"
      provides: "shadcn/ui configuration"
      contains: "aliases"
  key_links:
    - from: "shared/dashboard/vite.config.ts"
      to: "react"
      via: "external rollup option"
      pattern: "external.*react"
---

<objective>
Initialize the shared dashboard package with Vite + React + shadcn/ui foundation.

Purpose: Create a reusable React dashboard package in the monorepo that both objetiva-sync and objetiva-sync-gateway can consume.
Output: A buildable shared/dashboard package with shadcn/ui configured and ready for component development.
</objective>

<execution_context>
@C:\Users\Administrador\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Administrador\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-dashboard-modernization/17-CONTEXT.md
@.planning/phases/17-dashboard-modernization/17-RESEARCH.md
@shared/package.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite + React dashboard package</name>
  <files>
    shared/dashboard/package.json
    shared/dashboard/vite.config.ts
    shared/dashboard/tsconfig.json
    shared/dashboard/tsconfig.app.json
    shared/dashboard/src/main.tsx
    shared/dashboard/src/App.tsx
    shared/dashboard/src/index.css
    shared/dashboard/index.html
  </files>
  <action>
Create the shared/dashboard directory structure manually (not via create-vite to avoid extra files):

1. Create package.json with:
   - name: "@objetiva/dashboard"
   - type: "module"
   - React 18.3+ and react-dom as peerDependencies (NOT dependencies)
   - devDependencies: vite, @vitejs/plugin-react, typescript, @types/react, @types/react-dom, @types/node
   - scripts: "dev", "build", "preview"
   - exports: { ".": "./dist/index.js", "./styles": "./dist/style.css" }

2. Create vite.config.ts with:
   - @vitejs/plugin-react
   - @tailwindcss/vite plugin
   - Library mode build config (build.lib with entry src/index.ts)
   - Externalize react, react-dom, react/jsx-runtime in rollupOptions
   - Path alias: "@" -> "./src"

3. Create tsconfig.json with:
   - compilerOptions.baseUrl: "."
   - compilerOptions.paths: { "@/*": ["./src/*"] }
   - Strict mode enabled

4. Create tsconfig.app.json that extends tsconfig.json with app-specific settings

5. Create minimal src/App.tsx (placeholder component)
6. Create src/main.tsx entry for dev server
7. Create index.html for Vite dev server
8. Create src/index.css with Tailwind @import directive

Install dependencies via npm (from monorepo root):
```
npm install --workspace=shared/dashboard
```

IMPORTANT: Use unified radix-ui package (Feb 2026), NOT individual @radix-ui/react-* packages.
  </action>
  <verify>
Run from monorepo root:
```
cd shared/dashboard && npm run build
```
Build should complete without errors and produce dist/ folder with index.js and style.css
  </verify>
  <done>shared/dashboard package exists with React 18 + Vite configuration, compiles to library mode output</done>
</task>

<task type="auto">
  <name>Task 2: Configure shadcn/ui and add base utilities</name>
  <files>
    shared/dashboard/components.json
    shared/dashboard/src/lib/utils.ts
    shared/dashboard/tailwind.config.js
  </files>
  <action>
1. Install shadcn/ui prerequisites:
```
npm install --workspace=shared/dashboard clsx tailwind-merge class-variance-authority lucide-react
npm install --workspace=shared/dashboard tailwindcss @tailwindcss/vite
```

2. Initialize shadcn/ui configuration:
```
cd shared/dashboard && npx shadcn@latest init
```
When prompted:
- Style: Default
- Base color: Neutral (matches existing monorepo aesthetics)
- CSS variables: Yes
- Tailwind CSS config location: tailwind.config.js
- Components directory: src/components
- Utilities: src/lib/utils.ts
- React Server Components: No (this is client-side SPA)

3. Create src/lib/utils.ts manually if shadcn init doesn't create it:
```typescript
import { type ClassValue, clsx } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

4. Update src/index.css to include shadcn CSS variables (init should do this)

5. Create src/index.ts as the library entry point that exports components:
```typescript
// Core utilities
export { cn } from './lib/utils'

// Re-export components as they're added
```

IMPORTANT: After init, verify unified radix-ui package is used. Run:
```
npx shadcn@latest migrate radix
```
This ensures the Feb 2026 unified package is used.
  </action>
  <verify>
1. components.json exists with valid configuration
2. src/lib/utils.ts exports cn() function
3. Verify unified radix-ui package is used:
   - Run `grep -c "@radix-ui/react-" package.json` - should return 0 (no individual packages)
   - Run `npm ls radix-ui` - should show unified package is installed
4. Run `npm run build` from shared/dashboard - should succeed
  </verify>
  <done>shadcn/ui configured with components.json, utils.ts created, unified radix-ui package confirmed</done>
</task>

<task type="auto">
  <name>Task 3: Register shared/dashboard as workspace member</name>
  <files>
    package.json
    shared/dashboard/package.json
  </files>
  <action>
1. Update root package.json to include shared/dashboard in workspaces:
```json
{
  "workspaces": [
    "shared",
    "shared/dashboard",
    "objetiva-sync",
    "objetiva-sync-gateway"
  ]
}
```

2. Run npm install from monorepo root to link workspace:
```
npm install
```

3. Verify the workspace is recognized:
```
npm ls @objetiva/dashboard
```

4. Update objetiva-sync/package.json to add @objetiva/dashboard as dependency:
```json
{
  "dependencies": {
    "@objetiva/dashboard": "*"
  }
}
```

5. Install React in objetiva-sync (as regular dependency since dashboard expects it as peer):
```
npm install --workspace=objetiva-sync react react-dom
```

IMPORTANT: React must be installed in the consumer (objetiva-sync), not in shared/dashboard. This prevents duplicate React instances and "invalid hook call" errors.
  </action>
  <verify>
1. `npm ls @objetiva/dashboard` shows it linked correctly
2. `npm ls react` shows single instance shared across workspace
3. No "invalid hook call" warnings when importing from @objetiva/dashboard
  </verify>
  <done>shared/dashboard registered as workspace, objetiva-sync can import from @objetiva/dashboard</done>
</task>

</tasks>

<verification>
1. `cd shared/dashboard && npm run build` succeeds
2. `npm ls @objetiva/dashboard` shows workspace link
3. components.json exists with valid shadcn configuration
4. No duplicate React instances in node_modules tree
</verification>

<success_criteria>
- shared/dashboard package initializes and builds without errors
- shadcn/ui configured and ready for component addition
- Workspace properly linked in monorepo
- React externalized in library build (not bundled)
</success_criteria>

<output>
After completion, create `.planning/phases/17-dashboard-modernization/17-01-SUMMARY.md`
</output>
