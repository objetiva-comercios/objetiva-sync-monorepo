---
phase: 26-schema-comparison-api
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - objetiva-sync-gateway/tests/unit/sync-schema-store.test.ts
  - objetiva-sync-gateway/tests/unit/schema-comparison.test.ts
  - objetiva-sync-gateway/tests/integration/schema-comparison.integration.test.ts
autonomous: true
requirements: [SCHEMA-02, SCHEMA-04]

must_haves:
  truths:
    - "Unit tests exist for sync schema store set/get/hasData/reset operations"
    - "Unit tests exist for comparison logic: aligned, mismatched, missing, not_reported, summary counts"
    - "Integration tests exist for POST /api/schemas/report (auth, validation, storage)"
    - "Integration tests exist for GET /api/schemas/compare (all 4 entities, not_reported state)"
  artifacts:
    - path: "objetiva-sync-gateway/tests/unit/sync-schema-store.test.ts"
      provides: "Unit tests for in-memory sync schema store"
      min_lines: 40
    - path: "objetiva-sync-gateway/tests/unit/schema-comparison.test.ts"
      provides: "Unit tests for comparison logic functions"
      min_lines: 80
    - path: "objetiva-sync-gateway/tests/integration/schema-comparison.integration.test.ts"
      provides: "Integration tests for both route handlers"
      min_lines: 80
  key_links: []
---

<objective>
Create test scaffolds for the schema comparison API before any production code exists (Nyquist Wave 0).

Purpose: Tests define the expected behavior for Plans 01 and 02. They will fail initially (RED phase) and pass once production code is implemented.
Output: 3 test files covering unit and integration behaviors for SCHEMA-02 and SCHEMA-04 requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-schema-comparison-api/26-CONTEXT.md
@.planning/phases/26-schema-comparison-api/26-RESEARCH.md

<interfaces>
<!-- Key types the test scaffolds reference. Extracted from codebase. -->

From shared/types/schema-metadata.ts:
```typescript
export interface ColumnMetadata {
  column_name: string;
  data_type: string;
  is_nullable: boolean;
  default_value: string | null;
  ordinal_position?: number;
  column_comment?: string | null;
}

export interface ConstraintMetadata {
  constraint_name: string;
  constraint_type: 'PRIMARY KEY' | 'FOREIGN KEY' | 'UNIQUE' | 'CHECK';
  columns: string[];
}

export interface TableSchemaMetadata {
  entity: string;
  columns: ColumnMetadata[];
  constraints: ConstraintMetadata[];
}
```

From objetiva-sync-gateway/tests/integration/pairing.integration.test.ts (test infrastructure pattern):
```typescript
import { describe, it, expect, beforeAll, afterAll, beforeEach } from 'vitest'
import { buildApp } from '../../src/app.js'
import type { FastifyInstance } from 'fastify'

async function createTestApp() {
  process.env.JWT_SECRET = 'test-jwt-secret-for-pairing-tests-32chars'
  process.env.JWT_EXPIRES_IN = '1h'
  const app = await buildApp()
  await app.ready()
  const token = app.jwt.sign({ username: 'admin', authenticated: true })
  return { app, token }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit test scaffolds for sync schema store and comparison logic</name>
  <files>
    objetiva-sync-gateway/tests/unit/sync-schema-store.test.ts,
    objetiva-sync-gateway/tests/unit/schema-comparison.test.ts
  </files>
  <read_first>
    objetiva-sync-gateway/tests/unit/pairing-store.test.ts,
    shared/types/schema-metadata.ts,
    .planning/phases/26-schema-comparison-api/26-RESEARCH.md
  </read_first>
  <action>
Create two unit test files. These tests import modules that DO NOT EXIST YET — they will fail at import time. This is intentional (RED phase).

**File 1: `tests/unit/sync-schema-store.test.ts`**

Tests for `syncSchemaStore` imported from `../../src/services/sync-schema-store.js`:

1. `set()` stores schemas and `get()` retrieves by entity name — create a `TableSchemaMetadata` fixture with entity `'articulos'`, 2 columns (`id` integer not-null, `nombre` text nullable), 1 constraint (PRIMARY KEY on `id`). Call `set([fixture])`, then `get('articulos')` should return the fixture.
2. `set()` clears previous data before storing — call `set([articulosFixture])`, then `set([otherFixture])`, then `get('articulos')` should return `null`.
3. `get()` returns `null` for unknown entity — `get('nonexistent')` returns `null`.
4. `hasData()` returns `false` initially, `true` after `set()`.
5. `_resetForTest()` clears all data — after `set()`, call `_resetForTest()`, then `hasData()` returns `false`.

Use `beforeEach(() => { syncSchemaStore._resetForTest(); })` to isolate tests.

**File 2: `tests/unit/schema-comparison.test.ts`**

Tests for pure comparison functions imported from `../../src/services/schema-comparison.js`:

Import `buildEntityComparison` (the function that takes entity name, pgSchema, compiledSchema, syncSchema, syncReported boolean and returns an `EntityComparison` object).

Test cases (per D-07, D-08, D-09, D-10):

1. **aligned** — PostgreSQL has column `id` (integer, not-null), compiled has same, sync has same, `syncReported=true`. Expect field status `'aligned'`, summary `{ aligned: 1, mismatched: 0, missing: 0 }`.
2. **mismatched data_type** — PostgreSQL has `precio` (numeric, not-null), compiled has `precio` (text, not-null). Expect status `'mismatched'`.
3. **mismatched is_nullable** — PostgreSQL has `nombre` (text, nullable=true), compiled has `nombre` (text, nullable=false). Expect status `'mismatched'`.
4. **missing from compiled** — PostgreSQL has `nuevo_campo` (text, nullable), compiled does NOT have it. Expect status `'missing'`.
5. **missing from sync** — PostgreSQL has `campo_x`, compiled has it, sync does NOT (but syncReported=true). Expect status `'missing'`.
6. **not_reported** — `syncReported=false`. All fields that match PG+compiled should be `'aligned'` (2-way). `sync` should be `null` in each field row. Entity-level `sync_reported` should be `false`.
7. **summary counts** — 3 fields: 1 aligned, 1 mismatched, 1 missing. Expect `summary: { aligned: 1, mismatched: 1, missing: 1 }`.
8. **null pgSchema** (introspection failed) — pass `null` for pgSchema. Expect all fields from compiled/sync to have `postgresql: null` and status `'missing'`.

Use helper functions to create minimal `TableSchemaMetadata` and `TableSchema` fixtures with just the columns needed per test.
  </action>
  <verify>
    <automated>cd objetiva-sync-gateway && npx vitest run tests/unit/sync-schema-store.test.ts tests/unit/schema-comparison.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/sync-schema-store.test.ts` exists and contains `import { syncSchemaStore }` and at least 5 `it(` blocks
    - File `tests/unit/schema-comparison.test.ts` exists and contains `import { buildEntityComparison }` and at least 7 `it(` blocks
    - Both files fail at import time (module not found) — this is expected RED phase behavior
    - Both files use `describe`/`it`/`expect` from vitest
    - No `test.skip` or `test.todo` — all tests are real assertions that will pass once production code exists
  </acceptance_criteria>
  <done>Two unit test files exist with concrete assertions for store operations and comparison logic, failing only because production modules don't exist yet</done>
</task>

<task type="auto">
  <name>Task 2: Create integration test scaffold for schema comparison routes</name>
  <files>
    objetiva-sync-gateway/tests/integration/schema-comparison.integration.test.ts
  </files>
  <read_first>
    objetiva-sync-gateway/tests/integration/pairing.integration.test.ts,
    .planning/phases/26-schema-comparison-api/26-RESEARCH.md
  </read_first>
  <action>
Create integration test file following the exact pattern from `pairing.integration.test.ts`:

**Setup:** `createTestApp()` helper that sets `process.env.JWT_SECRET = 'test-jwt-secret-for-schema-comparison-32chars'`, `process.env.JWT_EXPIRES_IN = '1h'`, calls `buildApp()`, `await app.ready()`, generates a valid JWT token via `app.jwt.sign({ username: 'admin', authenticated: true })`. Returns `{ app, token }`.

**POST /api/schemas/report tests:**

1. **401 without token** — `app.inject({ method: 'POST', url: '/api/schemas/report', payload: { schemas: [] } })`. Expect `statusCode` 401.
2. **400 with invalid body** — POST with token, body `{ schemas: "not-an-array" }`. Expect `statusCode` 400, response body contains `"VALIDATION_ERROR"`.
3. **400 with empty schemas array** — POST with token, body `{ schemas: [] }`. Expect 400 (zod `.min(1)` validation).
4. **200 with valid body** — POST with token, body `{ schemas: [{ entity: 'articulos', columns: [{ column_name: 'id', data_type: 'integer', is_nullable: false, default_value: null }], constraints: [] }] }`. Expect `statusCode` 200, body `{ success: true }`.

**GET /api/schemas/compare tests:**

5. **401 without token** — `app.inject({ method: 'GET', url: '/api/schemas/compare' })`. Expect 401.
6. **200 returns array of 4 entities** — GET with token. Expect 200, body is array with length 4, each item has `entity`, `sync_reported`, `summary`, `fields` properties.
7. **sync_reported is false when no POST /api/schemas/report was called** — GET with token. Each entity should have `sync_reported: false`.
8. **After POST report, sync_reported becomes true** — POST valid schemas, then GET compare. Entities that were reported should have `sync_reported: true`.

**Each describe block** uses its own `app` instance (via `beforeAll`/`afterAll`) to avoid cross-contamination. Import `_resetForTest` from `../../src/services/sync-schema-store.js` and call in `beforeEach`.

NOTE: These tests WILL FAIL because the routes and modules don't exist yet. The import of `sync-schema-store` will fail. The routes won't be registered. This is expected Wave 0 behavior. Tests 6-8 also depend on a working database for `IntrospectionService` — they may need to be marked with a comment noting they require the gateway's PostgreSQL connection. For now, write them as straightforward assertions; Plan 01 will make them pass.
  </action>
  <verify>
    <automated>cd objetiva-sync-gateway && npx vitest run tests/integration/schema-comparison.integration.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/integration/schema-comparison.integration.test.ts` exists
    - Contains `import { buildApp } from '../../src/app.js'`
    - Contains at least 8 `it(` blocks covering both POST and GET endpoints
    - Uses `app.inject()` pattern (NOT real HTTP requests)
    - Uses `Authorization: Bearer ${token}` header pattern for authenticated requests
    - Contains `_resetForTest` import from sync-schema-store module
    - Tests fail because production modules don't exist yet (expected RED phase)
  </acceptance_criteria>
  <done>Integration test file exists with 8+ test cases for both route handlers, failing only because production routes and modules don't exist yet</done>
</task>

</tasks>

<verification>
All 3 test files exist. They fail at import/route-registration time because production code doesn't exist yet. Once Plans 01 and 02 complete, re-running these tests should show progress toward GREEN.
</verification>

<success_criteria>
- 3 test files created in the correct directories
- Unit tests cover: store CRUD, comparison aligned/mismatched/missing/not_reported/summary
- Integration tests cover: auth (401), validation (400), happy path (200), sync_reported state
- All tests are real assertions (no skip/todo)
- Tests fail only due to missing production modules (not due to test bugs)
</success_criteria>

<output>
After completion, create `.planning/phases/26-schema-comparison-api/26-00-SUMMARY.md`
</output>
