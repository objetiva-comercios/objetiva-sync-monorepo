---
phase: 08-sync-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - objetiva-sync/src/dashboard/routes/api/sync.ts
  - objetiva-sync/src/config/constants.ts
  - objetiva-sync/src/adapters/sqlserver/sqlserver-adapter.ts
  - objetiva-sync/nginx/objetiva-sync.conf
autonomous: true

must_haves:
  truths:
    - "SSE connection stays alive during long sync operations (no proxy timeout kills it)"
    - "SQL Server queries for 100K+ records do not timeout at the adapter level"
    - "Batch processing delay does not add excessive wall-clock time to sync duration"
  artifacts:
    - path: "objetiva-sync/src/dashboard/routes/api/sync.ts"
      provides: "SSE heartbeat mechanism keeping connection alive every 15s"
      contains: "heartbeat"
    - path: "objetiva-sync/src/config/constants.ts"
      provides: "Increased SQL Server timeout and reduced batch delay"
      contains: "DELAY_BETWEEN_BATCHES_MS"
    - path: "objetiva-sync/nginx/objetiva-sync.conf"
      provides: "Nginx reverse proxy config with SSE-friendly timeouts"
      contains: "proxy_read_timeout"
  key_links:
    - from: "objetiva-sync/src/dashboard/routes/api/sync.ts"
      to: "reply.raw.write"
      via: "heartbeat interval writing SSE comments"
      pattern: "heartbeat"
    - from: "objetiva-sync/nginx/objetiva-sync.conf"
      to: "proxy_read_timeout"
      via: "nginx config for sync service"
      pattern: "proxy_read_timeout 600"
---

<objective>
Fix the ~60s timeout root cause by adding SSE heartbeat to the sync stream endpoint, increasing SQL Server query timeout for large datasets, reducing excessive inter-batch delay, and creating an nginx config for the sync service with SSE-appropriate timeouts.

Purpose: The sync fails after ~60s regardless of batch size. Research identified the most likely culprit as a missing SSE heartbeat causing proxy/connection timeout. This plan addresses ALL timeout layers on the sync service side of the pipeline.

Output: SSE endpoint with 15s heartbeat, SQL Server requestTimeout increased to 120s, batch delay reduced from 500ms to 100ms, nginx config with 600s proxy_read_timeout.
</objective>

<execution_context>
@C:\Users\Administrador\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Administrador\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sync-reliability/08-RESEARCH.md
@objetiva-sync/src/dashboard/routes/api/sync.ts
@objetiva-sync/src/config/constants.ts
@objetiva-sync/src/adapters/sqlserver/sqlserver-adapter.ts
@objetiva-sync-gateway/nginx/sync-gateway.conf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SSE heartbeat and connection cleanup to sync stream endpoint</name>
  <files>
    objetiva-sync/src/dashboard/routes/api/sync.ts
  </files>
  <action>
In the GET /api/sync/stream handler, immediately after `reply.raw.writeHead(200, ...)` (around line 504), add an SSE heartbeat mechanism:

1. Create a heartbeat interval that writes SSE comment lines every 15 seconds:
```typescript
const HEARTBEAT_INTERVAL_MS = 15_000;
const heartbeatTimer = setInterval(() => {
  try {
    reply.raw.write(': heartbeat\n\n');
  } catch {
    clearInterval(heartbeatTimer);
  }
}, HEARTBEAT_INTERVAL_MS);
```

2. Register cleanup on connection close (right after the heartbeat setup):
```typescript
request.raw.on('close', () => {
  clearInterval(heartbeatTimer);
});
```

3. Before every `reply.raw.end()` call in the handler (there are 3: error cases at lines 524/537, success at line 708, and catch at line 718), add `clearInterval(heartbeatTimer)` immediately before `reply.raw.end()`.

IMPORTANT: The SSE comment format `: heartbeat\n\n` is per SSE spec -- lines starting with `:` are comments and are ignored by EventSource clients but keep the connection alive through proxies.

Do NOT change any other logic in this file. The heartbeat is additive.
  </action>
  <verify>
1. Read the modified file and confirm:
   - `heartbeatTimer` is created with `setInterval` at 15000ms
   - `request.raw.on('close', ...)` clears the interval
   - All `reply.raw.end()` calls are preceded by `clearInterval(heartbeatTimer)`
   - SSE comment format uses `: heartbeat\n\n` (colon prefix, double newline)
2. Run `npx tsc --noEmit --project objetiva-sync/tsconfig.json 2>&1 | head -20` to check for type errors (may have pre-existing errors from Phase 9 scope -- only verify no NEW errors introduced)
  </verify>
  <done>
SSE stream endpoint sends heartbeat comments every 15 seconds, preventing proxy timeout from killing the connection during long sync operations. All code paths properly clean up the heartbeat interval.
  </done>
</task>

<task type="auto">
  <name>Task 2: Increase SQL Server timeout, reduce batch delay, create nginx config</name>
  <files>
    objetiva-sync/src/config/constants.ts
    objetiva-sync/src/adapters/sqlserver/sqlserver-adapter.ts
    objetiva-sync/nginx/objetiva-sync.conf
  </files>
  <action>
**constants.ts changes:**
1. Change `DELAY_BETWEEN_BATCHES_MS` from `500` to `100` (line 15). Update the comment to explain: `// 100ms backpressure delay between batches (was 500ms, reduced for throughput)`
2. Change `QUERY_TIMEOUT_MS` from `30000` to `120000` (line 23). Update comment: `// 120 seconds (2 minutes) for large dataset queries`

**sqlserver-adapter.ts changes:**
1. Find the default `requestTimeout` values (currently `10000` at lines 36, 94, 180). Change ALL occurrences of the default from `10000` to `120000`. This gives SQL Server 2 minutes for large queries instead of 10 seconds.
2. Similarly update `connectionTimeout` defaults from `10000` to `30000` (30 seconds for connection establishment).
3. Update the Zod schema default on line 36: `.default(120000)` and line 35: `.default(30000)`.

WHY these values:
- requestTimeout 120s: A SELECT returning 100K+ rows from SQL Server over network can take 15-60 seconds. 10s is too aggressive.
- connectionTimeout 30s: Connection establishment should be fast but 10s can be tight on busy servers.
- batch delay 100ms: 500ms * 1000 batches = 500s of pure waiting. 100ms * 1000 = 100s. Still provides backpressure but 5x faster.

**nginx config (NEW file):**
Create `objetiva-sync/nginx/objetiva-sync.conf` modeled after `objetiva-sync-gateway/nginx/sync-gateway.conf` but with SSE-specific settings:
- Server name: `sync.sanchezrepuestos.com.ar` (placeholder -- user will adjust)
- Proxy to `http://127.0.0.1:3000` (default Fastify port for sync service)
- `proxy_read_timeout 600;` (10 minutes for SSE)
- `proxy_send_timeout 600;`
- `proxy_connect_timeout 60;`
- `proxy_buffering off;` (CRITICAL for SSE -- buffering kills SSE streaming)
- `proxy_cache off;`
- `proxy_set_header Connection '';` (for SSE, do NOT use "upgrade")
- `client_max_body_size 10M;`
- Include standard headers (Host, X-Real-IP, X-Forwarded-For, X-Forwarded-Proto)
- HTTP to HTTPS redirect block
- SSL certificate paths (placeholder with Let's Encrypt paths)
- Add comment at top explaining this is for the sync service SSE endpoint

Do NOT include WebSocket upgrade headers -- SSE does not use WebSocket.
  </action>
  <verify>
1. Read `objetiva-sync/src/config/constants.ts` and confirm:
   - `DELAY_BETWEEN_BATCHES_MS: 100`
   - `QUERY_TIMEOUT_MS: 120000`
2. Grep for `requestTimeout` in `sqlserver-adapter.ts` and confirm all defaults are `120000`
3. Grep for `connectionTimeout` in `sqlserver-adapter.ts` and confirm all defaults are `30000`
4. Read `objetiva-sync/nginx/objetiva-sync.conf` and confirm:
   - `proxy_read_timeout 600`
   - `proxy_buffering off`
   - No WebSocket upgrade headers
   - `proxy_set_header Connection ''`
  </verify>
  <done>
SQL Server can handle 100K+ row queries without timeout (120s). Batch delay is reduced 5x (500ms to 100ms). Nginx config exists for sync service with SSE-appropriate settings (proxy_buffering off, 600s read timeout).
  </done>
</task>

</tasks>

<verification>
1. SSE heartbeat is active in sync stream endpoint with 15s interval
2. SQL Server requestTimeout increased to 120s across all code paths
3. Batch delay reduced from 500ms to 100ms
4. Nginx config exists with proxy_buffering off and 600s timeouts
5. No TypeScript compilation errors introduced (pre-existing errors acceptable)
</verification>

<success_criteria>
- The sync SSE endpoint sends `: heartbeat\n\n` every 15 seconds during sync
- SQL Server adapter uses 120000ms requestTimeout by default
- Inter-batch delay is 100ms instead of 500ms
- Nginx config for sync service disables buffering and has 600s proxy_read_timeout
- All heartbeat intervals are properly cleaned up on connection close and stream end
</success_criteria>

<output>
After completion, create `.planning/phases/08-sync-reliability/08-01-SUMMARY.md`
</output>
