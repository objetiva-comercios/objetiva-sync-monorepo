---
phase: 23-fix-wizard-pairing-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - objetiva-sync-gateway/src/lib/system-state.ts
  - objetiva-sync-gateway/src/routes/setup.ts
  - objetiva-sync-gateway/src/routes/pairing.ts
  - objetiva-sync-gateway/src/utils/env-writer.ts
  - objetiva-sync-gateway/tests/integration/setup-wizard.integration.test.ts
  - objetiva-sync-gateway/tests/integration/wizard-flow.test.ts
autonomous: true
requirements:
  - AUTH-RM-04
  - AUTH-RM-05
  - PAIR-01
  - PAIR-02

must_haves:
  truths:
    - "POST /api/setup/token returns 200 with valid JWT after apply-config transitions to normal mode (setupComplete=false)"
    - "POST /api/setup/token returns 403 after a pairing code has been successfully claimed (setupComplete=true)"
    - "Full wizard flow completes end-to-end: preflight -> test-db -> save-domain -> generate-jwt -> apply-config -> setup/token -> pairing/generate"
    - "Existing gateway test suite passes without regressions"
  artifacts:
    - path: "objetiva-sync-gateway/src/lib/system-state.ts"
      provides: "setupComplete boolean property on systemState singleton"
      contains: "setupComplete"
    - path: "objetiva-sync-gateway/src/routes/setup.ts"
      provides: "Widened guard on /api/setup/token endpoint"
      contains: "setupComplete"
    - path: "objetiva-sync-gateway/src/routes/pairing.ts"
      provides: "setupComplete set to true on successful claim"
      contains: "systemState.setupComplete"
    - path: "objetiva-sync-gateway/tests/integration/wizard-flow.test.ts"
      provides: "Full wizard flow integration test"
      min_lines: 80
  key_links:
    - from: "objetiva-sync-gateway/src/routes/pairing.ts"
      to: "objetiva-sync-gateway/src/lib/system-state.ts"
      via: "import systemState, set setupComplete=true on claim"
      pattern: "systemState\\.setupComplete\\s*=\\s*true"
    - from: "objetiva-sync-gateway/src/routes/setup.ts"
      to: "objetiva-sync-gateway/src/lib/system-state.ts"
      via: "import systemState, read setupComplete in guard"
      pattern: "systemState\\.setupComplete"
---

<objective>
Fix the critical 403 bug in the wizard pairing flow where POST /api/setup/token fails after apply-config transitions startupMode to 'normal'. Add setupComplete flag to systemState, widen the token endpoint guard, lock it after first claim, update JSDoc, fix existing broken test, and write full wizard flow integration test.

Purpose: Without this fix, fresh install wizard cannot complete -- step 4 (token acquisition) fails with 403 after step 3 (apply-config) changes mode to normal.
Output: Working token endpoint that survives mode transitions, integration test proving the full wizard flow, no regressions.
</objective>

<execution_context>
@C:/Users/sistemas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sistemas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-fix-wizard-pairing-auth/23-CONTEXT.md
@.planning/phases/23-fix-wizard-pairing-auth/23-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From objetiva-sync-gateway/src/lib/system-state.ts:
```typescript
import type { PreflightCheck } from '../routes/preflight.js'

export const systemState = {
  dbConnected: false,
  dbError: null as string | null,
  startTime: new Date(),
  lastDbCheck: null as Date | null,
  startupMode: 'normal' as 'normal' | 'setup-only' | 'degraded',
  preflightChecks: [] as PreflightCheck[]
}
```

From existing test (setup-wizard.integration.test.ts line 189):
```typescript
it('returns 403 after setup completes (normal mode)', async () => {
  const savedMode = systemState.startupMode
  systemState.startupMode = 'normal'
  // ... expects 403 -- THIS WILL BREAK after guard change
  // Must also set systemState.setupComplete = true to get 403
})
```

From env-writer.ts line 70 (JSDoc to fix):
```typescript
 * @param key   Environment variable name (e.g. 'SYNC_PASSWORD')
 // Change to: (e.g. 'JWT_SECRET')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add setupComplete to systemState, fix token guard, wire claim handler, fix existing test</name>
  <files>
    objetiva-sync-gateway/src/lib/system-state.ts,
    objetiva-sync-gateway/src/routes/setup.ts,
    objetiva-sync-gateway/src/routes/pairing.ts,
    objetiva-sync-gateway/src/utils/env-writer.ts,
    objetiva-sync-gateway/tests/integration/setup-wizard.integration.test.ts
  </files>
  <behavior>
    - Test: POST /api/setup/token returns 200 when startupMode='setup-only' (existing, should still pass)
    - Test: POST /api/setup/token returns 200 when startupMode='normal' AND setupComplete=false (new -- the bug fix)
    - Test: POST /api/setup/token returns 403 when startupMode='normal' AND setupComplete=true (updated existing test)
  </behavior>
  <action>
    1. **system-state.ts**: Add `setupComplete: false` property to the systemState object, after `preflightChecks`. Add comment: "Whether a pairing code has been successfully claimed in this container lifecycle".

    2. **setup.ts** (~line 1510): Replace the guard on `/api/setup/token`:
       - BEFORE: `if (systemState.startupMode !== 'setup-only')` -> 403
       - AFTER: `const canIssueToken = systemState.startupMode === 'setup-only' || (systemState.startupMode === 'normal' && !systemState.setupComplete); if (!canIssueToken)` -> 403
       - Add code comment explaining why: "Allow token during setup-only mode OR normal mode before first claim (fixes 403 bug when apply-config transitions mode mid-wizard). After restart with valid config, wizard UI is not served so this path is not exposed."

    3. **pairing.ts**: Add `import { systemState } from '../lib/system-state.js'` at the top. In the claim handler, after `if (result === 'ok')` and after `lastCodeWasClaimed = true`, add `systemState.setupComplete = true` with comment "Lock /api/setup/token endpoint".

    4. **env-writer.ts** (line 70 JSDoc): Change `'SYNC_PASSWORD'` to `'JWT_SECRET'` in the @param example.

    5. **setup-wizard.integration.test.ts** (line ~189): Update the "returns 403 after setup completes (normal mode)" test to also set `systemState.setupComplete = true` before the inject call, and restore it in finally block. This is required because the guard now allows normal+!setupComplete.
  </action>
  <verify>
    <automated>cd objetiva-sync-gateway && npx vitest run tests/integration/setup-wizard.integration.test.ts -x</automated>
  </verify>
  <done>
    - systemState has setupComplete property initialized to false
    - /api/setup/token allows requests in setup-only mode
    - /api/setup/token allows requests in normal mode when setupComplete=false
    - /api/setup/token returns 403 in normal mode when setupComplete=true
    - Claim handler sets setupComplete=true on successful claim
    - env-writer.ts JSDoc shows JWT_SECRET instead of SYNC_PASSWORD
    - All existing setup-wizard tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create wizard flow integration test covering end-to-end flow</name>
  <files>
    objetiva-sync-gateway/tests/integration/wizard-flow.test.ts
  </files>
  <behavior>
    - Test: Full wizard flow succeeds: preflight(200) -> test-db -> save-domain -> generate-jwt -> apply-config(200) -> setup/token(200, valid JWT) -> pairing/generate(200, 6-char code)
    - Test: After claim, POST /api/setup/token returns 403 (setupComplete lockout)
    - Test: POST /api/setup/token returns 200 in normal mode before any claim
  </behavior>
  <action>
    Create `objetiva-sync-gateway/tests/integration/wizard-flow.test.ts`:

    1. Import buildApp from the app module, systemState from system-state.ts, vitest helpers.
    2. Mock the DB connection (Prisma) so preflight/test-db don't require a real PostgreSQL. Use the established pattern from existing integration tests -- stub the Prisma client or the specific endpoints that need DB.
    3. Create a describe block "Wizard flow end-to-end" with its own buildApp() instance (Phase 20 isolation pattern).
    4. In beforeEach, reset systemState: `systemState.startupMode = 'setup-only'`, `systemState.setupComplete = false`, `systemState.dbConnected = false`.

    **Test: full wizard flow**:
    - POST /api/setup/test-db with mock DB credentials -> 200 (mock the DB test to succeed)
    - POST /api/setup/save-domain with valid domain -> 200
    - POST /api/setup/generate-jwt -> 200 (generates JWT_SECRET)
    - POST /api/setup/apply-config -> 200 (this transitions startupMode to 'normal')
    - Verify `systemState.startupMode === 'normal'`
    - POST /api/setup/token -> 200 (THE BUG FIX: must work even though mode is 'normal')
    - Parse JWT from response, use it as Bearer token
    - POST /api/pairing/generate with Bearer token -> 200 with 6-char code

    **Test: token locked after claim**:
    - Set up: startupMode='normal', setupComplete=false
    - POST /api/setup/token -> 200 (works before claim)
    - Set systemState.setupComplete = true (simulating claim)
    - POST /api/setup/token -> 403

    **Test: token available in setup-only mode**:
    - Set startupMode='setup-only', setupComplete=false
    - POST /api/setup/token -> 200

    Use app.inject() for all HTTP calls (no port binding). Mock DB operations by mocking Prisma or the relevant service functions. Focus on HTTP flow and mode transitions per CONTEXT.md decision.
  </action>
  <verify>
    <automated>cd objetiva-sync-gateway && npx vitest run tests/integration/wizard-flow.test.ts -x</automated>
  </verify>
  <done>
    - wizard-flow.test.ts exists with 3+ test cases
    - Full wizard flow test passes: all steps 200, token works after apply-config
    - Token lockout test passes: 403 after setupComplete=true
    - Token available test passes: 200 in setup-only mode
    - All gateway tests pass: `cd objetiva-sync-gateway && npx vitest run`
  </done>
</task>

</tasks>

<verification>
1. `cd objetiva-sync-gateway && npx vitest run` — all tests pass (no regressions)
2. `grep -r "setupComplete" objetiva-sync-gateway/src/` — found in system-state.ts, setup.ts, pairing.ts
3. `grep "SYNC_PASSWORD" objetiva-sync-gateway/src/utils/env-writer.ts` — no matches (cleaned)
</verification>

<success_criteria>
- POST /api/setup/token returns 200 after apply-config transitions to normal mode (the bug is fixed)
- POST /api/setup/token returns 403 after a successful pairing claim (security lockout works)
- Full wizard flow integration test passes end-to-end
- All existing gateway tests pass without regressions
- env-writer.ts JSDoc no longer references SYNC_PASSWORD
</success_criteria>

<output>
After completion, create `.planning/phases/23-fix-wizard-pairing-auth/23-01-SUMMARY.md`
</output>
