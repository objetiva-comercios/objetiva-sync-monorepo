---
phase: 27-schema-status-page
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - objetiva-sync-gateway/dashboard/src/App.tsx
autonomous: false
requirements:
  - SCHEMA-01
  - SCHEMA-03

must_haves:
  truths:
    - "App.tsx renders a top tab bar with Dashboard and Schema Status tabs"
    - "Clicking Schema Status tab renders the SchemaStatus page instead of Dashboard"
    - "Default active tab is Dashboard (existing behavior preserved)"
    - "Tab bar uses border-b-2 border-primary for active tab indicator"
  artifacts:
    - path: "objetiva-sync-gateway/dashboard/src/App.tsx"
      provides: "Top-level tab navigation between Dashboard and Schema Status"
      contains: "SchemaStatus"
  key_links:
    - from: "objetiva-sync-gateway/dashboard/src/App.tsx"
      to: "objetiva-sync-gateway/dashboard/src/components/SchemaStatus.tsx"
      via: "conditional render based on activeTab state"
      pattern: "activeTab.*schema.*SchemaStatus"
    - from: "objetiva-sync-gateway/dashboard/src/App.tsx"
      to: "objetiva-sync-gateway/dashboard/src/components/Dashboard.tsx"
      via: "conditional render based on activeTab state"
      pattern: "activeTab.*dashboard.*Dashboard"
---

<objective>
Wire the Schema Status page into App.tsx with a top-level tab bar, and visually verify the complete feature.

Purpose: Completes the navigation integration (per D-01, D-02) so the operator can switch between Dashboard and Schema Status pages.
Output: Modified App.tsx with tab bar. Visual checkpoint confirms end-to-end rendering.
</objective>

<execution_context>
@C:/Users/sistemas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sistemas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-schema-status-page/27-CONTEXT.md
@.planning/phases/27-schema-status-page/27-UI-SPEC.md
@.planning/phases/27-schema-status-page/27-01-SUMMARY.md

<interfaces>
From objetiva-sync-gateway/dashboard/src/App.tsx (current state):
```typescript
import { Dashboard } from '@/components/Dashboard'

function App() {
  return <Dashboard />
}

export default App
```

From Plan 01 outputs:
```typescript
// src/components/SchemaStatus.tsx
export function SchemaStatus(): JSX.Element
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add top-level tab bar to App.tsx</name>
  <files>
    objetiva-sync-gateway/dashboard/src/App.tsx
  </files>
  <read_first>
    objetiva-sync-gateway/dashboard/src/App.tsx,
    objetiva-sync-gateway/dashboard/src/components/SchemaStatus.tsx,
    .planning/phases/27-schema-status-page/27-UI-SPEC.md
  </read_first>
  <action>
Replace the entire `App.tsx` content with the tab bar navigation (per D-01, D-02, UI-SPEC Navigation Change):

```typescript
import { useState } from 'react'
import { Dashboard } from '@/components/Dashboard'
import { SchemaStatus } from '@/components/SchemaStatus'
import { cn } from '@/lib/utils'

function App() {
  const [activeTab, setActiveTab] = useState<'dashboard' | 'schema'>('dashboard')

  return (
    <div>
      {/* Top tab bar — per D-01, D-02 */}
      <nav className="bg-card border-b border-border">
        <div className="max-w-[1600px] mx-auto px-4 flex gap-0">
          {(['dashboard', 'schema'] as const).map((tab) => (
            <button
              key={tab}
              onClick={() => setActiveTab(tab)}
              className={cn(
                'px-6 min-h-[44px] text-sm font-medium border-b-2 transition-colors',
                activeTab === tab
                  ? 'border-primary text-primary font-bold'
                  : 'border-transparent text-muted-foreground hover:text-foreground'
              )}
            >
              {tab === 'dashboard' ? 'Dashboard' : 'Schema Status'}
            </button>
          ))}
        </div>
      </nav>
      {/* Page content — conditional render per D-01 */}
      {activeTab === 'dashboard' ? <Dashboard /> : <SchemaStatus />}
    </div>
  )
}

export default App
```

Key points:
- Default tab is `'dashboard'` (preserves existing behavior)
- Tab bar is `bg-card border-b border-border` (matches UI-SPEC)
- Each tab has `min-h-[44px]` (WCAG 2.5.5 touch target)
- Active: `border-primary text-primary font-bold`; Inactive: `border-transparent text-muted-foreground hover:text-foreground`
- `max-w-[1600px] mx-auto px-4` matches Dashboard content width
- No router — simple `useState` toggle per D-01
  </action>
  <verify>
    <automated>cd objetiva-sync-gateway/dashboard && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `App.tsx` contains `import { SchemaStatus }` from `@/components/SchemaStatus`
    - `App.tsx` contains `import { Dashboard }` from `@/components/Dashboard`
    - `App.tsx` contains `useState<'dashboard' | 'schema'>('dashboard')`
    - `App.tsx` contains `min-h-[44px]` (WCAG touch target)
    - `App.tsx` contains `border-primary text-primary font-bold` (active tab)
    - `App.tsx` contains `Schema Status` as tab label text
    - `App.tsx` contains `activeTab === 'dashboard' ? <Dashboard /> : <SchemaStatus />`
    - Vite build completes without errors
  </acceptance_criteria>
  <done>App.tsx has top-level tab bar with Dashboard (default) and Schema Status tabs. Conditional rendering switches pages without router.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Schema Status page</name>
  <files>objetiva-sync-gateway/dashboard/src/App.tsx</files>
  <action>
Human visually verifies the complete Schema Status feature in the browser. No code changes — verification only.

What was built across Plan 01 and Plan 02:
- Top-level tab bar (Dashboard / Schema Status) in App.tsx
- Entity tabs with summary badges (problem count or green checkmark)
- Comparison table with columns: Campo, PostgreSQL, Compilado, Sync, Estado
- Color-coded status indicators (green/red/yellow dots and row tints)
- Sync not reported banner (conditional)
- Loading, error, and empty states

Verification steps:
1. Start the gateway: `cd objetiva-sync-gateway && npm run dev`
2. Open browser to the dashboard URL (typically http://localhost:3000 or the configured port)
3. Verify the top tab bar shows "Dashboard" and "Schema Status" tabs
4. Click "Schema Status" tab
5. Verify the Schema Status page loads with entity tabs and comparison table
6. Click through each entity tab — verify table updates and badge counts are correct
7. Verify status indicators: green dots for aligned fields, red for mismatched, yellow for missing
8. Verify row background tints match status (subtle red/yellow for problem rows)
9. Click "Dashboard" tab — verify existing dashboard still works normally
10. Click "Actualizar" button — verify data refreshes
  </action>
  <verify>
    <automated>cd objetiva-sync-gateway/dashboard && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms Schema Status tab renders comparison table
    - Human confirms entity tab switching works
    - Human confirms color-coded status indicators are visible
    - Human confirms Dashboard tab still works
  </acceptance_criteria>
  <done>Human approved the visual and functional behavior of the Schema Status page. SCHEMA-01 and SCHEMA-03 are confirmed.</done>
</task>

</tasks>

<verification>
- `cd objetiva-sync-gateway/dashboard && npx vite build` exits 0
- App.tsx imports both Dashboard and SchemaStatus
- Tab bar renders with correct active/inactive styles
- Visual checkpoint confirms full feature works end-to-end
</verification>

<success_criteria>
Operator can navigate to Schema Status page via top tab bar, view all entity comparison tables with color-coded alignment indicators, and switch between entities. Dashboard tab still works. SCHEMA-01 and SCHEMA-03 are visually confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/27-schema-status-page/27-02-SUMMARY.md`
</output>
