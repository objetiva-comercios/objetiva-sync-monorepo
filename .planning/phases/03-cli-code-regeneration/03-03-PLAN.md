---
phase: 03-cli-code-regeneration
plan: 03
type: execute
wave: 1
depends_on: ["03-01", "03-02"]
files_modified:
  - objetiva-sync-gateway/.env
autonomous: false

must_haves:
  truths:
    - "CLI command npm run regenerate-schemas successfully authenticates with gateway and fetches schemas"
    - "CLI generates files and writes them to correct paths (prisma/schema.prisma, shared/schemas/generated/*.ts)"
    - "CLI runs prisma generate successfully and output shows 'Generated Prisma Client'"
    - "CLI --dry-run flag shows diffs but does NOT write files or run prisma generate"
    - "CLI --entity flag processes only the specified entity"
  artifacts:
    - path: "objetiva-sync-gateway/.env"
      provides: "GATEWAY_URL, SYNC_USERNAME, SYNC_PASSWORD environment variables"
    - path: "objetiva-sync-gateway/prisma/schema.prisma"
      provides: "Regenerated Prisma schema with current database structure"
    - path: "objetiva-sync-gateway/shared/schemas/generated/"
      provides: "Generated Zod schema files for each entity"
  key_links:
    - from: "scripts/regenerate-schemas.ts"
      to: "gateway /auth/login endpoint"
      via: "Authentication with credentials from .env"
      pattern: "POST.*auth/login"
    - from: "src/codegen/index.ts"
      to: "gateway /api/schemas/:entity endpoints"
      via: "Fetch schema metadata with JWT token"
      pattern: "GET.*api/schemas"
---

<objective>
Close Phase 3 verification gaps by running end-to-end tests of the CLI regeneration tool.

Purpose: The code is complete but untested due to missing environment setup. This plan sets up the required environment (gateway running, credentials configured) and executes the 3 critical verification tests to confirm the phase goal is achieved.

Output: Documented verification that CLI successfully authenticates, fetches schemas, generates files, writes them, and runs prisma generate.
</objective>

<execution_context>
@C:\Users\Administrador\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Administrador\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cli-code-regeneration/03-VERIFICATION.md
@.planning/phases/03-cli-code-regeneration/03-CONTEXT.md
@objetiva-sync-gateway/scripts/regenerate-schemas.ts
@objetiva-sync-gateway/src/codegen/index.ts
</context>

<tasks>

<task type="human">
  <name>Task 1: Environment Setup - Start Gateway and Configure Credentials</name>
  <files>
    objetiva-sync-gateway/.env
  </files>
  <action>
    **This task requires human action to set up the environment.**

    **Prerequisites check:**
    1. Verify PostgreSQL database is accessible
    2. Verify gateway can connect to database
    3. Verify valid admin credentials are available

    **If prerequisites are met:**

    **Step 1: Configure environment variables**

    Edit or create `objetiva-sync-gateway/.env` with the following values:
    ```
    GATEWAY_URL=http://localhost:3001
    SYNC_USERNAME=admin
    SYNC_PASSWORD=<your_admin_password>
    ```

    Replace `<your_admin_password>` with the actual admin password for the gateway.

    **Step 2: Start the gateway server**

    Open a terminal and run:
    ```bash
    cd objetiva-sync-gateway
    npm run dev
    ```

    Wait for the gateway to start successfully. You should see output indicating:
    - Server listening on port 3001
    - Database connection established
    - No startup errors

    **Step 3: Verify gateway is responding**

    Test the gateway health endpoint:
    ```bash
    curl http://localhost:3001/health
    ```

    Expected: HTTP 200 response

    **If prerequisites are NOT met (database unavailable, no credentials, etc.):**

    Document the blocker in the task output. This is acceptable - the gap will be documented as "blocked by environment" and can be closed later when infrastructure is available.

    **Keep the gateway running for Tasks 2-4.**
  </action>
  <verify>
    - Gateway server is running on http://localhost:3001
    - .env file exists with GATEWAY_URL, SYNC_USERNAME, SYNC_PASSWORD
    - Gateway responds to health check
    - OR: Blocker documented with specific reason (no DB, no credentials, etc.)
  </verify>
  <done>
    Gateway is running and accessible at http://localhost:3001 with credentials configured in .env. Ready for CLI testing.
    OR: Environment setup blocked - documented reason in summary.
  </done>
</task>

<task type="human">
  <name>Task 2: Full Pipeline End-to-End Test (CRITICAL)</name>
  <files>
    objetiva-sync-gateway/prisma/schema.prisma
    objetiva-sync-gateway/shared/schemas/generated/*.ts
  </files>
  <action>
    **This task requires human action to run the CLI and observe results.**

    **Prerequisite:** Task 1 must be complete (gateway running)

    **Test execution:**

    Run the full regeneration command:
    ```bash
    cd objetiva-sync-gateway
    npm run regenerate-schemas 2>&1 | tee regenerate-output.txt
    ```

    **Observe and document the following:**

    1. **Authentication phase:**
       - Look for: "Authenticating with gateway..."
       - Look for: "Authentication successful" OR error message
       - If authentication fails, check credentials in .env

    2. **Schema fetching phase:**
       - Look for: "Fetching schema for [entity]..." messages
       - Count how many entities are processed (should be 4: articulos, comprobantes_cabecera, comprobantes_detalle, comprobantes_pagos)
       - Look for: "Fetched N schema(s)" summary

    3. **Diff display phase:**
       - Look for colored diff output with +/- lines
       - Note if changes were detected or "No changes detected"

    4. **File writing phase:**
       - Look for: "Writing files..." OR "--dry-run: No files were modified"
       - Check file timestamps of prisma/schema.prisma and shared/schemas/generated/*.ts
       - Verify files were updated (timestamps changed)

    5. **Prisma generate phase (CLI-03 - CRITICAL):**
       - Look for: "Running prisma generate..."
       - **CRITICAL:** Look for: "Generated Prisma Client" in output
       - If this message appears, CLI-03 is VERIFIED
       - If this message does NOT appear, CLI-03 is FAILED

    6. **Exit code:**
       - Check: `echo $?` (should be 0 for success)

    **Document results:**

    Create a file `regenerate-output.txt` with the full console output.

    Document in task notes:
    - ✓ or ✗ for each phase above
    - Specific error messages if any phase failed
    - File paths and timestamps if files were written

    **Success criteria for this test:**
    - All 6 phases completed successfully
    - "Generated Prisma Client" message appeared (proves CLI-03)
    - Exit code 0
    - Files written with recent timestamps
  </action>
  <verify>
    - Command executed without errors (exit code 0)
    - Output includes "Generated Prisma Client" message
    - prisma/schema.prisma file has recent timestamp
    - shared/schemas/generated/ directory contains .generated.ts files
    - regenerate-output.txt captures full console output
    - OR: Test failed - specific errors documented
  </verify>
  <done>
    Full pipeline test completed successfully. CLI authenticated, fetched 4 entity schemas, displayed diffs, wrote files, and ran prisma generate. Output shows "Generated Prisma Client" confirming CLI-03.
    OR: Test failed - documented errors and blockers in summary.
  </done>
</task>

<task type="human">
  <name>Task 3: Dry-Run Mode Verification</name>
  <files>
    objetiva-sync-gateway/prisma/schema.prisma
  </files>
  <action>
    **This task requires human action to verify --dry-run behavior.**

    **Prerequisite:** Task 1 must be complete (gateway running)

    **Test execution:**

    **Step 1: Note current file timestamps**
    ```bash
    cd objetiva-sync-gateway
    ls -l prisma/schema.prisma
    ls -l shared/schemas/generated/*.ts
    ```

    Record the modification times.

    **Step 2: Run with --dry-run flag**
    ```bash
    npm run regenerate-schemas -- --dry-run 2>&1 | tee dry-run-output.txt
    ```

    **Step 3: Verify dry-run behavior**

    **Observe and document:**
    1. Look for: "Mode: DRY RUN (no files will be modified)" message
    2. Look for: Diff output (diffs should still be displayed)
    3. Look for: "--dry-run: No files were modified" message
    4. **CRITICAL:** Verify NO "Running prisma generate..." message
    5. **CRITICAL:** Verify NO "Generated Prisma Client" message

    **Step 4: Check file timestamps**
    ```bash
    ls -l prisma/schema.prisma
    ls -l shared/schemas/generated/*.ts
    ```

    Compare with timestamps from Step 1. They should be UNCHANGED.

    **Document results:**
    - Diffs displayed: Yes/No
    - Files modified: Yes/No (should be No)
    - prisma generate executed: Yes/No (should be No)
    - Timestamps unchanged: Yes/No (should be Yes)

    **Success criteria:**
    - Diffs displayed
    - "--dry-run: No files were modified" message appeared
    - NO "Running prisma generate..." message
    - File timestamps unchanged
  </action>
  <verify>
    - dry-run-output.txt shows diffs but no file writes
    - "--dry-run: No files were modified" message in output
    - NO "Running prisma generate..." in output
    - File timestamps unchanged from before dry-run
    - Exit code 0
  </verify>
  <done>
    Dry-run mode verified. CLI displayed diffs but did not modify any files and did not run prisma generate. File timestamps unchanged.
    OR: Test failed - documented issues in summary.
  </done>
</task>

<task type="human">
  <name>Task 4: Entity-Specific Regeneration Verification</name>
  <files>
    objetiva-sync-gateway/shared/schemas/generated/articulos.generated.ts
  </files>
  <action>
    **This task requires human action to verify --entity flag behavior.**

    **Prerequisite:** Task 1 must be complete (gateway running)

    **Test execution:**

    Run with --entity flag for a single entity:
    ```bash
    cd objetiva-sync-gateway
    npm run regenerate-schemas -- --entity articulos 2>&1 | tee entity-output.txt
    ```

    **Observe and document:**

    1. Look for: "Entity filter: articulos" message
    2. Count schema fetch messages - should be only 1: "Fetching schema for articulos..."
    3. Verify NO fetch messages for other entities (comprobantes_cabecera, comprobantes_detalle, comprobantes_pagos)
    4. Check which files were updated (only articulos.generated.ts should change)

    **Verify file changes:**
    ```bash
    # Note: This might update schema.prisma too (since it's a complete schema file)
    # But only articulos model should have changes in the diff
    ls -l shared/schemas/generated/articulos.generated.ts
    ```

    **Document results:**
    - Entity filter applied: Yes/No
    - Number of entities processed: (should be 1)
    - Only articulos schema fetched: Yes/No
    - Files updated: (list file paths)

    **Success criteria:**
    - "Entity filter: articulos" message in output
    - Only 1 entity processed
    - No fetch messages for other entities
    - Exit code 0
  </action>
  <verify>
    - entity-output.txt shows "Entity filter: articulos"
    - Only 1 "Fetching schema for..." message (for articulos)
    - Exit code 0
    - Command completed successfully
  </verify>
  <done>
    Entity-specific regeneration verified. CLI processed only articulos entity when --entity flag was used. Other entities were not fetched.
    OR: Test failed - documented issues in summary.
  </done>
</task>

<task type="human">
  <name>Task 5: Document Verification Results and Update Status</name>
  <files>
    .planning/phases/03-cli-code-regeneration/03-UAT.md
  </files>
  <action>
    **This task requires human judgment to assess overall verification status.**

    Create `.planning/phases/03-cli-code-regeneration/03-UAT.md` with the following structure:

    ```markdown
    # Phase 3: CLI Code Regeneration - User Acceptance Testing

    **Tested:** [current date/time]
    **Tester:** [your name or "Claude + Human"]
    **Environment:** [local development / staging / production]

    ## Test Results Summary

    | Test | Status | Notes |
    |------|--------|-------|
    | Full Pipeline E2E | [PASS/FAIL/BLOCKED] | [notes] |
    | Dry-Run Mode | [PASS/FAIL/BLOCKED] | [notes] |
    | Entity-Specific | [PASS/FAIL/BLOCKED] | [notes] |

    ## Verification Status

    **Phase Goal:** Developer can regenerate Prisma and Zod schemas from PostgreSQL with single command

    **Goal Achieved:** [YES/NO/BLOCKED]

    **Requirements Status:**
    - CLI-01 (npm run regenerate-schemas introspects PostgreSQL): [VERIFIED/FAILED/BLOCKED]
    - CLI-02 (Generates/updates schema.prisma): [VERIFIED/FAILED/BLOCKED]
    - CLI-03 (Runs prisma generate after update): [VERIFIED/FAILED/BLOCKED]
    - CLI-04 (Generates Zod schemas): [VERIFIED/FAILED/BLOCKED]
    - CLI-05 (Displays diff summary): [VERIFIED/FAILED/BLOCKED]
    - CLI-06 (Supports dry-run mode): [VERIFIED/FAILED/BLOCKED]
    - CLI-07 (Supports entity-specific flag): [VERIFIED/FAILED/BLOCKED]

    ## Detailed Test Results

    ### Test 1: Full Pipeline End-to-End

    [Paste relevant output from regenerate-output.txt]

    **Observations:**
    - [bullet points of what was observed]

    **Issues Found:**
    - [any errors or unexpected behavior]

    ### Test 2: Dry-Run Mode

    [Paste relevant output from dry-run-output.txt]

    **Observations:**
    - [bullet points]

    ### Test 3: Entity-Specific Regeneration

    [Paste relevant output from entity-output.txt]

    **Observations:**
    - [bullet points]

    ## Gaps Closed

    ✓ CLI command end-to-end execution verified
    ✓ prisma generate execution confirmed (CLI-03)
    ✓ Generated files written to correct paths

    ## Remaining Gaps (if any)

    [List any gaps that could not be closed due to blockers]

    ## Recommendations

    [Any suggestions for Phase 4 or future improvements]

    ---
    *Testing complete: [date/time]*
    ```

    Fill in all bracketed sections based on the actual test results from Tasks 2-4.

    **If environment setup was blocked (Task 1 failed):**
    Document all tests as BLOCKED with reason. The UAT report should clearly state that verification could not be completed due to environment constraints and will need to be completed when infrastructure is available.

    **If all tests passed:**
    Update the phase status to VERIFIED and mark CLI-01 through CLI-07 requirements as complete.
  </action>
  <verify>
    - 03-UAT.md file created
    - All test results documented
    - Requirements status clearly marked
    - Phase goal achievement status documented
    - Gaps closure status documented
  </verify>
  <done>
    UAT report created documenting all verification test results. Phase 3 verification status updated based on test outcomes.
  </done>
</task>

</tasks>

<verification>
After all 5 tasks complete:

**If all tests passed:**
- Phase 3 goal is VERIFIED: "Developer can regenerate Prisma and Zod schemas from PostgreSQL with single command"
- All 7 CLI requirements (CLI-01 through CLI-07) are VERIFIED
- Gaps from verification report are CLOSED
- 03-UAT.md documents successful testing

**If tests were blocked:**
- Phase 3 remains CODE-COMPLETE but VERIFICATION-BLOCKED
- Specific blockers documented in 03-UAT.md
- Gaps remain OPEN with documented reasons
- Verification deferred until environment is available

**If tests failed:**
- Failures documented in 03-UAT.md with specific error messages
- Root cause analysis included
- Follow-up tasks or plans identified
</verification>

<success_criteria>
- Environment setup attempted (gateway start, credentials configuration)
- All 3 verification tests executed OR blockers documented
- Test results captured in output files (regenerate-output.txt, dry-run-output.txt, entity-output.txt)
- 03-UAT.md created with comprehensive test results
- Phase verification status updated based on test outcomes
- If successful: "Generated Prisma Client" message confirmed, proving CLI-03 works
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-code-regeneration/03-03-SUMMARY.md`
</output>
