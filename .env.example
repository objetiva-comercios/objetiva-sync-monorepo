# Optional — for manual override of schema regeneration auth.
# After pairing, the script auto-discovers these from the gateway's .env.
# Only set these if auto-discovery doesn't work (e.g., remote dev machine without gateway .env).
# GATEWAY_URL=http://your-gateway-url:3335
# JWT_SECRET=your-jwt-secret-here
